# من تطبيقٍ واحد إلى منظومة

هذا الملف يقول أين نحن وما التالي. الويب الحالي في `src/` **يعمل ولا
يُلمس** حتى الخطوة الأخيرة.

## ما جرى (الخطوة صفر)

| الطبقة | الحالة |
| --- | --- |
| `pnpm-workspace.yaml` | مساحات عمل: `apps/*` و`packages/*` |
| `packages/shared` | ثوابت ومخطّطات Zod يتقاسمها الخادم والموبايل والويب |
| `packages/db` | عميل Prisma واحد فوق المخطّط القائم |
| `apps/api` | Hono: بيئةٌ مفحوصة، رؤوس أمان، CORS بقائمة، حدّ معدّل، تحقّق Zod، JWT + تجديدٌ بتدوير |
| `apps/web` | هيكل: `(public)` و`(admin)` |
| `apps/mobile` | هيكل Expo: هوية، وبابُ خادمٍ بتجديدٍ واحد |
| `docker/` | صورةٌ للخادم و`compose` لقاعدةٍ لا تُكشف للعالم |

الخدمات المُجهَّزة: مشروعا Sentry (`athar-api` و`athar-mobile`)،
ومشروع PostHog (`ATHAR`، توقيت الرياض).

## ما جرى (الخطوات ١–٥)

| الطبقة | الحالة |
| --- | --- |
| القراءة | `feed` و`circle` و`store` و`media` — شكلٌ مسطّح وصفحاتٌ بمؤشّر |
| الكتابة | النشر والتفاعل والتعليق والصداقة والتصنيفات والحظر والملف |
| الرؤية | `visibleWhere` نفسه يحكم القراءة والكتابة — و«غير موجودة» لمن لا يرى |
| الملفات | رابطٌ مؤقّت إلى R2 ثم اعتمادٌ بعد فحص البايتات: sharp ينزع البيانات الوصفية، وffprobe يقيس المدّة |
| المحادثات | HTTP للكتابة، وWebSocket للاستماع — المصادقة في المصافحة، والدفع تسريعٌ لا تخزين |
| الموبايل | الشاشات الخمس والدخول ولحظةٌ وملفُّ صديق — على الخادم نفسه، بالهوية نفسها |
| الفلترة والبلاغ | قائمةُ كلماتٍ تُفحص بها اللحظات والتعليقات والرسائل، وبلاغٌ على كل منشورٍ وقصةٍ ورسالة، ويُحسم في اللوحة بيد |
| الويب الموحّد | اللوحة منقولةً كما هي، والصفحات العامة: الخصوصية والشروط والتواصل وحذف الحساب — شروط المتجرين |

## الترتيب

1. ~~**الخطوة ١ — القراءة**~~ ✓
2. ~~**الخطوة ٢ — الكتابة**~~ ✓
3. ~~**الخطوة ٣ — الملفات**~~ ✓
4. ~~**الخطوة ٤ — المحادثات**~~ ✓
5. ~~**الخطوة ٥ — الموبايل**~~ ✓: الشاشات كلها — الخط الزمني والنشر
   والرفع والقصص والمحادثات والمتجر والملف والإشعارات والخصوصية.
6. ~~**الخطوة ٦ — الويب الموحّد**~~ ✓: اللوحة منقولةٌ كما هي إلى
   `apps/web/src/app/(admin)/` وفيها قسما البلاغات والكلمات الممنوعة،
   والصفحات العامة مبنيّة في `(public)`: الهبوط وعن آثار والخصوصية
   والشروط والتواصل والوظائف وحذف الحساب. والويب الحالي في `src/` يبقى
   يعمل حتى الخطوة ٩.
7. **الخطوة ٧ — الدفع والاقتصاد**: RevenueCat مع آثار+. الكود مبنيّ على الطرفين
   وموثّق في `BILLING.md`: باب الحدث في الخادم، والشراء والاستعادة في
   التطبيق، وتفعيلٌ لا يمنحه إلا الخادم. ويُجرَّب الآن على
   **متجر RevenueCat التجريبي** لأنّ ربط آبل يطلب `.p8` ولا يوجد قبل
   حساب المطوّرين. ويبقى للخطوة ٨: حساب آبل، فالمنتجان في المتجرين،
   فمفتاحا `appl_`/`goog_`.
   ومعه **اقتصاد النقاط**: عملة المتجر الوحيدة، والاشتراك وحده بمالٍ
   حقيقي ويمنح ١٠٠٠ نقطة شهرياً. والباقات تُدار من اللوحة، والشحن يمرّ
   بالمتجرين (`NON_RENEWING_PURCHASE`) وسجلُّه `CoinTopUp`. مبنيٌّ
   ومُجرَّب على قاعدة، ويبقى: منتجات الشحن في المتجرين ومعرّفاتها.
8. **الخطوة ٧٫٥ — تدقيق أمني** قبل المتجرين: مراجعة كل مسار ومدخل
   واستعلام، واختبار اختراق، وتقسية الخادم.
   جرى منه: كل مسارٍ في `apps/api` خلف `requireAuth` (عدا الدخول
   والتسجيل والتجديد، وهي محدودة المعدّل)، والمشرف يُقرأ من صفّه لا من
   توكنه فلا يبقى مشرفاً ربع ساعةٍ بعد سحب صلاحيته، وباب الملفات صار
   يفحص الصلاحية (`canSeeMedia`) في الخادم والويب معاً — وكان في الويب
   مفتوحاً بلا حساب.
   ومعها **تأكيد البريد**: مزوّدُ بريدٍ صادر، ورابطُ تأكيدٍ للعنوان
   الجديد، وعمودُ `emailVerified`، وإشعارُ العنوان *القديم* بالتغيير —
   فمن سُرق حسابه يرى النقل في بريده. اليوم كلمةُ المرور وحدها هي
   الحارس، وهذا يكفي ما دام لا بريد صادر، ولا يكفي بعده.
9. **الخطوة ٨ — النشر**: VPS بـDocker، ونطاقٌ للـAPI. واليوم الخادم
   على Render من هذا الفرع — يكفي للتجربة، ولا يكفي للإنتاج: الخطّة
   خادمٌ نملكه، ومكانُه قرارٌ نظاميّ لا تقنيّ (حماية البيانات السعودي).
10. **الخطوة ٩ — التقاعد**: يُطفأ `src/` بعد أن يعمل الموبايل والويب
    الموحّد في الإنتاج.

## قواعد لا تُكسر

- الموبايل لا يستورد Prisma. أبداً.
- المسار يتحقّق ثم يستدعي خدمة. لا منطق في المسارات.
- `userId` من التوكن وحده — لا من الجسد ولا الاستعلام.
- كل استعلام يُقيَّد بصاحبه.
- المخطّط واحد في `prisma/` حتى نهاية الترحيل: مخطّطان لقاعدةٍ واحدة
  يعني هجرتين تتعاركان.
