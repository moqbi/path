import "server-only";

export type TagView = { name: string; bg: string; fg: string };

/*
 * وسم المشتركين التلقائي («داعم») انتهى: صار الاشتراك نجمةً بجانب الاسم
 * (`NameTag`)، أصغر ولا تحتاج ترجمةً مع الأسماء اللاتينية. وما بقي من
 * الوسوم يُمنح من اللوحة لشخصٍ بعينه ويُقرأ من `user.tag` مباشرة.
 */
