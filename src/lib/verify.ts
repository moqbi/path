/**
 * مهلةُ تأكيد البريد — نسخةُ `UNVERIFIED_MINUTES` في `packages/shared`.
 *
 * ومكانُها هنا لأنّ هذا المشروع بـnpm لا يرى حزمَ pnpm، ولأنّ مكوّنَ
 * العميل يقرؤها ليقول للمستخدم كم بقي له (كالقاعدة ١١٧: ما يقرؤه
 * الطرفان يجلس وحده). وهما رقمٌ واحد، فإن تغيّر تغيّرا معاً.
 *
 * والكنسُ نفسه في خادم الواجهة وحده — يعمل دائماً، فلا يُعلَّق على
 * وصول طلبٍ إلى الويب.
 */
export const UNVERIFIED_MINUTES = 10;
