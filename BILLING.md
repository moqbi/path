# الدفع — آثار+ عبر RevenueCat

المسار كله في سطر: يشتري المستخدم من **App Store** أو **Google Play**،
و**RevenueCat** يجمع الإيصالين ويُرسل لنا حدثاً، والخادم وحده يفعّل
«آثار+». ولا يُصدَّق الجهاز: قولُه «اشتريتُ» لا يفتح شيئاً.

## ما بُني

| الطرف | الملف | ما يفعله |
| --- | --- | --- |
| الخادم | `apps/api/src/routes/v1/webhooks.ts` | `POST /v1/webhooks/revenuecat` — يقارن ترويسة `Authorization` بوقتٍ ثابت |
| الخادم | `apps/api/src/services/billing.ts` | يطبّق الحدث: يفعّل، يوقف، ويتجاهل المكرّر. ومعه `dripPlusCredit` يودع الرصيد الشهري |
| القاعدة | `BillingEvent` | معرّف كل حدث عولج — التسليم «مرّةً على الأقل» فقد يصل مرّتين |
| التطبيق | `apps/mobile/lib/billing.ts` | الباقات والشراء والاستعادة وفتح إدارة الاشتراك في المتجر |
| التطبيق | `apps/mobile/app/subscribe.tsx` | الأسعار من المتجر، وانتظارُ التفعيل من الخادم بعد الشراء |

**والرصيد شهريّ لا مع الفاتورة**: الوعد في الشاشة «١٠٠٠ نقطة شهرياً»،
ومن اشترك سنوياً يفوتر مرّةً واحدة — فربطُ الإيداع بحدث الفوترة كان
يعطيه دفعةً بدل اثنتي عشرة. الآن الدفعة الأولى مع التفعيل، وما بعدها
يجري مع الكنس الدوريّ كل ثلاثين يوماً ما دام الاشتراك حيّاً، والختم
(`User.plusCreditAt`) يتقدّم شهراً لا يُضبط على اللحظة — فخادمٌ نام
يومين يُعطي صاحبه ما فاته لا يبتلعه.

والحالة تُقرأ من `expiration_at_ms` لا من نوع الحدث: **«ألغى» تعني أوقف
التجديد ويبقى مشتركاً إلى نهاية مدّته** — ومن يسلبه اشتراكه لحظةَ
الإلغاء يسلبه ما دفع ثمنه.

## الإعداد — بالترتيب

حساب المطوّرين مفتوح، فهذه الخطوات تُنفَّذ الآن. **وترتيبُها ليس
اقتراحاً**: كلُّ خطوةٍ تحتاج ما قبلها، ومعرّفُ المنتج يُكتب في ثلاثة
مواضع فإن اختلف حرفٌ لم يُعثر عليه.

### ١) منتجاتٌ في App Store Connect

`appstoreconnect.apple.com` ← التطبيق **آثار مومنتس**.

**أ — اشتراك آثار+**: القائمة الجانبية ← **Subscriptions** ← أنشئ
**Subscription Group** واحدة (اسمها لك، مثلاً `Athar Plus`)، ثمّ فيها
اشتراكين:

| المنتج | Product ID المقترح | المدّة |
| --- | --- | --- |
| شهريّ | `athar_plus_monthly` | 1 Month |
| سنويّ | `athar_plus_yearly` | 1 Year |

ولكلٍّ سعرٌ ووصفٌ واسمُ عرضٍ ولقطةُ مراجعة — آبل لا تعتمد منتجاً ناقصاً.

**ب — باقات النقاط**: **In-App Purchases** ← نوعُ كلٍّ **Consumable**
(تُشترى مرّاتٍ، لا تُستعاد). معرّفٌ لكلّ باقةٍ عندك، مثلاً
`athar_coins_1000`.

> والاشتراك **Auto-Renewable** والنقاط **Consumable**: النوعُ يُختار
> مرّةً ولا يُبدَّل بعدها، ومنتجٌ بنوعٍ خاطئ يُحذف ويُعاد بمعرّفٍ آخر.

### ٢) RevenueCat

`app.revenuecat.com` ← مشروعك (أو أنشئه).

**أ — التطبيق**: Project Settings ← **Apps** ← + ← App Store:
- Bundle ID: `app.athar.mobile`
- **App Store Connect API Key**: نفسُ المفتاح الذي رفعتَه إلى Expo —
  أو أنشئ ثانياً من المكان نفسه. (يقرأ به أسعارك وحالةَ اشتراكاتك.)

**ب — الاستحقاق**: **Entitlements** ← + ← المعرّف **`athr_pro`**
بالضبط. مكتوبٌ في `packages/shared/src/constants.ts` ولا يُغيَّر —
تغييرُه يكسر كلَّ اشتراكٍ قائم.

**ج — المنتجات**: **Products** ← استورد المنتجين من آبل، واربط كلاً
منهما بالاستحقاق `athr_pro`. (باقاتُ النقاط **لا تُربط باستحقاق**: نقاطٌ
تُودَع لا صفةٌ تُلبَس.)

**د — العرض**: **Offerings** ← عرضٌ معرّفه `default` واجعله **Current**،
وفيه باقتان:

| Package | Identifier | المنتج |
| --- | --- | --- |
| Monthly | `$rc_monthly` | `athar_plus_monthly` |
| Annual | **`$rc_annual`** | `athar_plus_yearly` |

> **والسنوية تُعرف بنوعها لا باسمها**: الشاشة تقرأ
> `packageType === "ANNUAL"` (`lib/billing.ts` ← `plans()`). فباقةٌ
> سنويّةٌ أُضيفت كـ«Custom» تظهر في القائمة بلا وسمِ «سنوي» — وهذا
> تفسيرُ ما لوحظ من أنّ الباقة السنوية لا تظهر كما يجب.

**هـ — مفتاح SDK**: Project Settings ← **API Keys** ← انسخ مفتاح
**Apple App Store** العامّ (`appl_…`). يُكتب في `eas.json` ←
`production.env.EXPO_PUBLIC_RC_IOS_KEY` — و**يحتاج بناءً جديداً**،
فالقيمة تُدمج في الحزمة لا تُقرأ عند التشغيل.

**و — الويب هوك**: **Integrations** ← Webhooks ← +:
- URL: `https://atharmts.com/v1/webhooks/revenuecat`
- Authorization header: **نفسُ** قيمة `REVENUECAT_WEBHOOK_SECRET`
  على الخادم حرفاً بحرف.

### ٣) باقات النقاط في لوحتنا

`atharmts.com/admin?s=store&v=packs` ← لكلّ باقةٍ اكتب في حقل **`sku`**
معرّفَ المنتج **كما هو في App Store Connect** (`athar_coins_1000`).
وباقةٌ بلا `sku` تبقى مسوّدةً لا تُعرض — فلا يُضغط زرٌّ لا يفتح نافذة
شراء (القاعدة ٧٣ج).

### ٤) فتحُ بيئة التجربة على الخادم — مؤقّتاً

الكود يُهمل كلَّ حدثٍ بيئتُه `SANDBOX`، فالشراءُ في TestFlight ينجح على
الجهاز والحدثُ يُرمى بسطرٍ في السجلّ:

```bash
cd ~/app && sed -i 's|^ALLOW_SANDBOX_BILLING=.*|ALLOW_SANDBOX_BILLING="true"|' .env
sudo systemctl restart athar-api
```

> **ويُعاد إلى `false` قبل أن يدخل مستخدمٌ حقيقيّ.** المشروع الواحد في
> RevenueCat يجمع التجريبيّ والحقيقيّ، فحدثٌ تجريبيّ يفتح آثار+ لحسابٍ
> حقيقيّ بضغطةٍ في نافذةٍ وهمية.

### ٥) حساب تجربة عند آبل

`appstoreconnect.apple.com` ← **Users and Access** ← تبويب **Sandbox**
← **Test Accounts** ← +: بريدٌ **لم يُستعمل** في أيّ حساب آبل.

وعلى الآيفون: **Settings ← Apps ← App Store** ← انزل إلى
**Sandbox Account** ← سجّل به. ولا تسجّل به في إعدادات iCloud.

### ٦) البناء والتجربة

بناءٌ جديد بعد أن يُكتب `EXPO_PUBLIC_RC_IOS_KEY`، ثمّ في التطبيق:

| ما تجرّبه | ما يجب أن يقع |
| --- | --- |
| شاشة الاشتراك | تعرض السعر **من المتجر** لا من الكود، وباقتين إحداهما موسومةٌ «سنوي» |
| شراء الشهريّ | نافذةُ آبل تقول **[Environment: Sandbox]**، ثمّ يُفتح آثار+ |
| شراء نقاط | يزيد الرصيد، ويظهر صفٌّ في `CoinTopUp` |
| فتحُ التطبيق ثانيةً | الاشتراك باقٍ — يُقرأ من الخادم لا من الجهاز |

وإن اشتريتَ ولم يُفتح شيء، فالسببُ في السجلّ لا في التخمين:

```bash
journalctl -u athar-api -n 120 --no-pager | grep -iE "revenuecat|فوترة|تجريبيّ"
```

- `حدث فوترةٍ تجريبيّ مُهمَل` ← الخطوة ٤ لم تُنفَّذ.
- لا شيء إطلاقاً ← الويب هوك لم يصل: راجع العنوان والترويسة (الخطوة ٢و).
- `لا حساب لهذا المعرّف` ← الجهاز اشترى قبل أن تُربط الجلسةُ بحسابك.

## المتجر التجريبي — ما نعمل عليه الآن

| | التجريبي | الحقيقي |
| --- | --- | --- |
| المفتاح | `EXPO_PUBLIC_RC_TEST_KEY` | `EXPO_PUBLIC_RC_IOS_KEY` · `EXPO_PUBLIC_RC_ANDROID_KEY` |
| الشراء | نافذةٌ وهمية في التطبيق | نافذة آبل/جوجل |
| الحدث | يصل الخادم فعلاً (`store: "TEST_STORE"`) | `APP_STORE` · `PLAY_STORE` |
| التجديد | خمس مرّاتٍ ثم يتوقّف (الشهري كل ٥ دقائق) | بمدّته |

فيُختبر المسار كلّه قبل أن يوجد حساب آبل: الشراء، ثم الحدث، ثم التفعيل
في القاعدة. والمفتاح الحقيقي يسبق التجريبي في الكود، فيوم يوجد
`appl_`/`goog_` يعمل بلا تغيير سطر.

والمفتاح التجريبي مكتوبٌ في `eas.json` لأنّه **مفتاح عميل** يُدفن في
نسخة التطبيق بحكم عمله — لا سرَّ فيه كسرّ الترويسة. ولو أردته خارج
المستودع فمكانه متغيّرات بيئة EAS في لوحة Expo، ويُحذف من الملف.

**وحارسٌ لازم**: المشروع الواحد في RevenueCat يجمع المتجرين والتجريبيّ،
فحدثٌ تجريبيّ يصل بنفس الترويسة إلى نفس الباب. ولذلك يرفض الخادم في
الإنتاج كلَّ حدثٍ من `TEST_STORE` أو بيئةِ `SANDBOX`
(`ALLOW_SANDBOX_BILLING` مطفأة تلقائياً حين `NODE_ENV=production`) —
وإلا فتحت نسخةٌ تجريبية آثار+ لحسابٍ حقيقيّ بضغطةٍ في نافذةٍ وهمية.

وتقول الشاشة صراحةً «متجرٌ تجريبيّ — الشراء هنا وهميّ»، فلا يُحسب شراءً.

و**المفتاح التجريبي للتطوير وحده**: في بناء الإصدار تكشفه الحزمة نفسها
وتُغلق التطبيق («Wrong API Key … The app will close now») حمايةً لبيانات
الشراء التجريبية — يُغلق على الشاشة الأولى فلا يُفتح أصلاً. ولذلك
حارسان: `__DEV__` في `lib/billing.ts` يُهمل المفتاح خارج التطوير، وهو
محذوفٌ من `preview` و`production` في `eas.json`. ونسخةُ `preview`
تعرض «الاشتراك يتمّ من داخل التطبيق…» بلا أسعار، والشراء التجريبي
يُجرَّب في نسخة `development` على خادم Metro.

### ما لم يُفحص بعد

الشراء نفسه. `react-native-purchases` حزمةٌ أصليّة لا تعمل في معاينة
الويب ولا Expo Go، فأوّل تجربةٍ حقيقية تكون على نسخة EAS:

```bash
cd apps/mobile && eas build --profile development --platform android
```

وفيها يُفتح «آثار+» ← تظهر الباقتان بأسعار المتجر التجريبي ← شراءٌ
بنافذةٍ وهمية ← يصل الحدث إلى الخادم ← يُفعَّل الاشتراك. وما فُحص حتى
الآن هو الطرف الذي يقرّر: الخادم.

## ما لا يعمل قبل البناء

`react-native-purchases` حزمةٌ أصليّة: لا تعمل في معاينة الويب ولا في
Expo Go، بل في نسخة تطوير مبنيّة بـEAS. وحتى ذلك الحين تعرض شاشة
الاشتراك سطراً يقول إنّ الشراء من التطبيق على الجوّال، وزرَّي تجربةٍ
يردّهما الخادم ما لم تُفتح `ALLOW_FAKE_PLUS`.

## ما فُحص فعلاً

بخادمٍ محلّي وسرٍّ تجريبي، بحمولاتٍ على شكل حمولة RevenueCat:

- بلا ترويسة، وبترويسةٍ خاطئة ← `401`.
- `INITIAL_PURCHASE` ← فُعّل آثار+، وأُودع ٣٠ ر.س.
- نفس الحدث ثانيةً ← «مكرّر»، ولا رصيد ثانٍ.
- `CANCELLATION` ← يبقى مشتركاً إلى نهاية مدّته، بلا إيداعٍ جديد.
- `EXPIRATION` ← أُوقف، والرصيد المكتسب يبقى.
- معرّفٌ مجهول (`$RCAnonymousID:…`) ← «لا حساب لهذا المعرّف».
- `TEST` من لوحتهم ← `200`.
- `POST /v1/plus` بحساب مستخدم ← `403` ما دامت `ALLOW_FAKE_PLUS` مطفأة.
- حدثٌ باستحقاقٍ غير `athr_pro` ← «استحقاق آخر»، بلا آثار.
- الرصيد الشهري: بعد يومٍ لا شيء، وبعد ٣١ يوماً دفعة، وبعد ٩٥ يوماً
  ثلاث دفعاتٍ فائتة، ولمنتهي الاشتراك لا شيء.
- بخادمَين معاً: حدثُ `TEST_STORE` وحدثُ `SANDBOX` رُفضا على خادم
  الإنتاج وقُبلا على خادم التطوير، وحدثُ `APP_STORE/PRODUCTION` قُبل على
  الإنتاج.
