# تدقيقٌ قبل النقل — آثار مومنتس

تاريخُه: ٢١ سبتمبر ٢٠٢٦. أُجري على `main` عند `b613305`.

**ما فُحص بالفعل**: الكودُ كلُّه قراءةً، والمخطّطُ بجداوله الخمسة
والثلاثين، ومسارات الخادم وصلاحياتُها، ومسارات الويب في متصفّحٍ حقيقيّ،
وسجلّات DNS للنطاق، وحساب Cloudflare.

**وما لم يُفحص — ويجب أن يُعرف قبل قراءة ما تحته**:

- **تطبيقُ الجوّال لم يُبنَ ولم يُشغَّل على جهازٍ في هذا العمل كلّه.**
  كلُّ ما فيه مرّ على المترجم (`tsc`) وحده. والبناءُ الناجح ليس دليلاً
  على أنّ الشاشة تعمل — وهذا مكتوبٌ في `CLAUDE.md` نفسه.
- **البريد لم يخرج مرّةً**: منفذ SMTP مغلقٌ في بيئة العمل.
- **التنبيهات لم تصل جهازاً**: تحتاج مفاتيحَ في EAS ونسخةً مبنيّة.
- **لا قياسَ أداءٍ على بياناتٍ حقيقية**: القاعدةُ هنا بذرةٌ من عشرات
  الصفوف، فما يلي عن الأداء **قراءةُ استعلامات** لا `EXPLAIN` على مليون
  صفّ.

---

## ١. الملخّص التنفيذي

المنتجُ مبنيٌّ أكثر ممّا هو مجرَّب. الميزاتُ الاثنتا عشرة المطلوبة
موجودةٌ كلُّها في الخادم والويب، وتسعٌ منها لها شاشاتٌ في الجوّال لم
تُشغَّل بعد. والأمانُ أمتنُ ممّا يُتوقَّع في هذه المرحلة: كلُّ مسارٍ
يفحص صلاحيته بنفسه، والرؤيةُ تُفحص في الاستعلام لا في الواجهة، والردُّ
«غير موجود» لا «ممنوع». ولم أجد ثغرةً تفتح بيانات أحدٍ لغيره.

والعوائقُ الحقيقيّة ثلاثة، وليس منها الكود: **قاعدةٌ تنتهي في ٨
أكتوبر**، و**تطبيقٌ لم يُجرَّب على جهاز**، و**لا اختبارات آليّة
إطلاقاً** — فكلُّ إصلاحٍ يُراجَع بالعين.

ووجدتُ **عيباً واحداً يستحقّ الإصلاح قبل النقل** (ترقيمُ الصفحات
بالمؤشّر يُسقط لحظات)، وثلاثةً تستحقّ الإصلاح قبل أن يكبر الاستعمال،
وبقيّتُها تأجيلٌ مقبول.

---

## ٢. الجدول

| # | المشكلة | الفئة | الخطورة | الأولوية | الحلّ |
|---|---|---|---|---|---|
| ١ | **ترقيمُ الخط الزمنيّ يُسقط لحظات**: `orderBy: createdAt` مع `cursor: {id}`، ولحظتان في اللحظة نفسها (وهو حالُ البذرة وحالُ النشر المتقارب) تجعلان الصفحة التالية تتخطّى أو تُكرّر | كود | عالية | **قبل النقل** | `orderBy: [{createdAt:"desc"},{id:"desc"}]` في `timeline` و`privateTimeline` و`momentsOf` وكلّ ما يُرقَّم |
| ٢ | **تفاعلاتُ اللحظة تُقرأ كلُّها** في كل صفحة خطّ زمنيّ: لا `take` على `reactions` في `shape`، فلحظةٌ بمئةٍ وخمسين تفاعلاً تجلبها كلَّها × عشرين لحظة | أداء | عالية | **قبل النقل** | `take: 12` مع `_count.reactions`، والشاشةُ تعرض الوجوه والعدد |
| ٣ | **لا اختبارات آليّة** (لا وحدة ولا تكامل ولا e2e) | كود | عالية | **قبل النقل** (الأهمّ خمسةً) | انظر §٥ |
| ٤ | **حدُّ المعدّل في الذاكرة** لا في Redis: نسختان من الخادم تعني ضِعفَ الحدّ، وإعادةُ التشغيل تصفّره | أمان | متوسطة | بعد النقل | استبدال المخزن بـRedis — الواجهةُ هي هي |
| ٥ | **حدُّ المعدّل على الدخول والبريد فقط**: النشرُ والتعليقُ والرسائل بلا حدّ، فحسابٌ واحد يستطيع أن يُغرق القاعدة | أمان | متوسطة | بعد النقل | `rateLimit` على مسارات الكتابة بمفتاح المستخدم لا العنوان |
| ٦ | **ختمُ الحضور كتابةٌ في `User` كلّ دقيقة** لكلّ نشط | أداء | متوسطة | بعد النقل | Redis، ويُنزَّل كلّ بضع دقائق |
| ٧ | **الإشعارات تُشتقّ بثمانية استعلامات** في كلّ فتحة تبويب | أداء | متوسطة | بعد النقل | خبيئةٌ ٣٠–٦٠ ثانية |
| ٨ | **الملفّات تمرّ بالتطبيق**: بندُ النطاق الأكبر وأثقلُ ما على الخادم | أداء | متوسطة | قرارُ مالك | عاملُ حافّةٍ بتوقيعٍ قصير — يمسّ القاعدة ١٠٣ |
| ٩ | **`@sentry/node` مثبَّتٌ ولا يُستعمل**: لا `Sentry.init` في الكود | كود | منخفضة | بعد النقل | إمّا تفعيلُه بـ`sendDefaultPii: false`، وإمّا نزعُه |
| ١٠ | **`src/lib/tags.ts` لا يستورده شيء** | كود | منخفضة | بعد النقل | حذفُه |
| ١١ | **`.env.example` ناقصٌ ثلاثةً**: `ALLOW_FAKE_PLUS` و`ALLOW_SANDBOX_BILLING` و`REVENUECAT_WEBHOOK_SECRET` | بنية | منخفضة | **قبل النقل** | إضافتُها — بيئةٌ ناقصةٌ تُكتشف بعد النشر |
| ١٢ | **`EXPO_PUBLIC_API_URL` يشير إلى Render** في ملفّي `preview` و`production` | بنية | عالية | **مع النقل** | تبديلُه إلى `https://atharmts.com` (أو نطاقِ الخادم الفرعيّ) |
| ١٣ | **لا سجلّ `A` للنطاق** | بنية | عالية | **مع النقل** | يُضاف حين يوجد عنوان الخادم |
| ١٤ | **`StoreItem` بلا فهرس** بينما يُرتَّب بـ`sortOrder` ويُفلتر بـ`hidden` | قاعدة | منخفضة | بعد النقل | `@@index([hidden, sortOrder])` — والجدول صغيرٌ اليوم |
| ١٥ | **`User` بلا فهرسٍ على `lastSeenAt`** وشاشاتُ الحضور تقرؤه | قاعدة | منخفضة | مؤجَّل | يُقاس أوّلاً؛ الأرجح ألّا يلزم |
| ١٦ | **«البثّ» (broadcasts) غير موجود** بهذا الاسم | مواصفات | — | يُوضَّح | إن كان المقصود بثَّ الملفّات فهو مبنيٌّ بـ٢٠٦؛ وإن كان إعلاناتٍ عامّة فالحسابُ المفتوح (٣٢ب) يؤدّيها |
| ١٧ | **لا تأكيدَ بريدٍ إلزاميّ**: الحسابُ يعمل بلا تأكيد | متاجر | منخفضة | مقصود | قرارٌ مكتوب: حسابٌ قائمٌ لا يُقفل لأنّ رسالةً لم تصل |
| ١٨ | **لا وضعَ ليلي** | UX | — | مقصود | أُلغي بطلب المالك (القاعدة ٤٦) |

---

## ٢ب. ما أُصلح في هذا التدقيق

| # | ما جرى |
|---|---|
| ١ | **الترقيم**: صار `orderBy: [{createdAt},{id}]` في أربعة استعلاماتٍ مُرقَّمة — والمعرّفُ فريدٌ فيحسم التساوي |
| ٢ | **سقفُ التفاعلات**: `take: 12` مع `_count.reactions`، والشاشةُ تقول «وكذا غيرهم» — كان ثلاثةَ آلاف صفٍّ في تمريرةٍ واحدة |
| ٣ | **أربعةُ ملفّات اختبار، ٢١ حالة** بـ`vitest`: الرؤية، ورموزُ البريد، والوضعُ الهادئ، وشمُّ الصور المتحرّكة. و`npm run api:test` من الجذر |
| ١١ | **`.env.example`** اكتمل بالثلاثة الناقصة |

## ٣. ما يجب إصلاحه قبل النقل

1. **ترقيمُ الصفحات** (#١) — عيبُ صحّةٍ يُسقط محتوى المستخدمين.
2. **سقفُ التفاعلات في الخط الزمنيّ** (#٢) — تُصلح في سطر.
3. **`.env.example`** (#١١) — بيئةٌ ناقصةٌ تُكتشف متأخّرة.
4. **الاختبارات** (#٣) — أربعةٌ منها كُتبت، والخامس (الترقيم) يحتاج
   قاعدةَ اختبارٍ حقيقية فيُكتب مع أوّل بيئةٍ على الخادم.
5. **نقلُ القاعدة قبل ٨ أكتوبر** — الموعدُ النهائيّ الوحيد.

والأربعةُ الأولى **أُنجزت في هذا التدقيق** (§٢ب)، فلم يبقَ قبل النقل
إلا القاعدةُ نفسها و#١٢ و#١٣ اللذان يحتاجان عنوانَ الخادم.

## ٤. ما يُؤجَّل بعد النقل

حدُّ المعدّل في Redis وعلى مسارات الكتابة (#٤، #٥)، وختمُ الحضور (#٦)،
وخبيئةُ الإشعارات (#٧)، وSentry (#٩)، والملفّ اليتيم (#١٠)، وفهرسُ
المتجر (#١٤). كلُّها تنفع عند عشرات الآلاف، ولا شيء منها يمنع الإطلاق.

## ٥. الاختبارات الخمسة الموصى بها

بـ`vitest` على الخادم — لا متصفّح ولا جهاز:

1. **الرؤية** (`visibleWhere`): لحظةٌ لتصنيفٍ لا يراها من ليس فيه،
   ولحظةٌ لأشخاصٍ بأعيانهم لا يراها غيرُهم. هذا أخطرُ ما في المنظومة.
2. **الشراء** (`buyItem`): رصيدٌ ناقص يُردّ، والمخفيُّ لا يُشترى،
   والخصم والتمليك في معاملةٍ واحدة.
3. **الرموز** (`consume`): مرّةً واحدة، وينتهي بوقته، ويُردّ المنتهي
   برسالةٍ تقول «انتهى» لا «غير موجود».
4. **الوضع الهادئ** (`inQuietHours`): المدّةُ العابرة لمنتصف الليل —
   وهذه مكتوبةٌ ومجرَّبةٌ يدوياً اليوم، فتُثبَّت.
5. **الترقيم** بعد إصلاح #١: عشرون لحظةً بالطابع الزمنيّ نفسه تُقرأ
   على صفحتين بلا تكرارٍ ولا إسقاط.

## ٦. ما لا يستحقّ الإصلاح

- **نسخُ الملفّات بين `src/` و`apps/web/`**: مؤقّتٌ مقصود حتى يتقاعد
  الويب الحاليّ، وتوحيدُه الآن عملٌ يُرمى.
- **`console.log` الثمانية**: كلُّها أخبارُ تشغيلٍ (نُقل كذا ملفاً،
  الخادمُ على المنفذ) ولا تطبع بيانات أحد. وتحذيرُ البريد يطبع الرابط
  عمداً في بيئةٍ بلا مفتاح.
- **`dangerouslySetInnerHTML` في صفحة الارتداد**: محتواه `JSON.stringify`
  لرابطٍ نبنيه نحن، لا مدخلَ مستخدمٍ فيه.
- **٣٧ اعتمادية في الجوّال**: كلُّها مستعمَلة.

---

## ٧. تفصيلُ ما فُحص

### الميزات

| الميزة | الخادم | الويب | الجوّال | ملاحظة |
|---|---|---|---|---|
| اللحظات (صورٌ بلا فيديو) | ✅ | ✅ | ✅ غير مجرَّب | |
| القصص (صورة/فيديو + فلتر، ٢٤ ساعة) | ✅ | ✅ | ✅ غير مجرَّب | الانتهاء من `expiresAt` في الاستعلام |
| الرسائل (نصّ/صورة/صوت) | ✅ | ✅ | ✅ غير مجرَّب | إيصالٌ بثلاث حالات |
| الدائرة (١٥٠، أصدقاء الأصدقاء) | ✅ | ✅ | ✅ غير مجرَّب | `assertRoomForBoth` يحرس السقف |
| المتجر (إطارات/ثيمات/تمائم/حزم) | ✅ | ✅ | ✅ غير مجرَّب | |
| آثار+ وخصمُ ٢٠٪ | ✅ | ✅ | ✅ غير مجرَّب | التفعيلُ من RevenueCat وحده |
| النقاط (شحنٌ وشراء) | ✅ | ✅ | ✅ غير مجرَّب | لا بابَ منحٍ في الخادم — مقصود |
| الإبلاغ + الفلترة + الحظر | ✅ | ✅ | ✅ غير مجرَّب | البلاغُ على لحظةٍ وقصةٍ ورسالةٍ وحساب |
| تأكيد البريد + استعادة الكلمة | ✅ | ✅ مجرَّب | ✅ غير مجرَّب | الإرسالُ نفسه لم يُجرَّب |
| التنبيهات | ✅ | — | ✅ غير مجرَّب | تحتاج مفاتيحَ في EAS |
| الدخول بمزوّد | ✅ | ✅ | ✅ غير مجرَّب | ينتظر معرّفات العملاء |
| البثّ (broadcasts) | ؟ | ؟ | ؟ | لا شيء بهذا الاسم — انظر #١٦ |

### الأمان

**مَتين**: `scrypt` بملحٍ ١٦ بايتاً ومفتاحٍ ٦٤ ومقارنةٍ ثابتة الزمن؛
ومفتاحا JWT **منفصلان** وكلاهما ٣٢ حرفاً فأكثر؛ ورموزُ التجديد مجزّأةٌ
بعائلات؛ وكلُّ مسارٍ في الخادم خلف `requireAuth` إلا الويب-سوكِت وهو
يقرأ التوكن بنفسه؛ والكتابةُ خلف `requireActive`؛ والرفعُ يفحص الصيغة
والحجم **ويشمّ بايتات الملفّ** بعد الرفع لا يصدّق ما قاله العميل؛
و`canSeeMedia` في أبواب الملفّات الثلاثة؛ والبلاغُ فريدٌ لكل مُبلِغٍ
وهدف؛ وحدُّ المعدّل على الدخول والتسجيل والبريد؛ وCORS بقائمةٍ مغلقة؛
ولا أسرارَ في المستودع (`.gitignore` يمنع `*.p8` وحساب الخدمة).

**ما يحتاج عملاً**: #٤ و#٥ أعلاه.

### المتاجر

شروطُ آبل الخمسة مبنيّةٌ كلُّها: فلترةُ محتوى، وإبلاغٌ على كلّ منشور،
وحظرٌ في الاتجاهين، ووسيلةُ تواصلٍ داخل التطبيق ومن الموقع، وحذفُ
الحساب داخل التطبيق ومن الموقع. و«الدخول بحساب آبل» مبنيٌّ وهو إلزاميٌّ
متى عُرض مزوّدٌ آخر. وسياسةُ الخصوصية وشروطُ الاستخدام صفحتان في
التطبيق وعلى الموقع.

**الناقص للنشر**: الدفعُ عبر IAP مربوطٌ بمنتجاتٍ حقيقيّة، واستمارةُ
Data Safety في جوجل بلاي، والتصنيفُ العمريّ، واللقطات، وفيديو مراجعة
سناب.

### الهوية والنصوص

الألوانُ والخطوطُ واحدةٌ في الويب والجوّال (`theme/tokens` مقابل
`@theme` في `globals.css`)، والتدرّجُ في الشعار والفعل الأساسيّ وحدهما،
والاتجاهُ من اليمين مفروضٌ من الجذر. ولم أجد في الواجهة نصّاً إنجليزياً
ظاهراً للمستخدم، ولا `Lorem`، ولا `TODO` متروكة — ما ظهر في الفحص
كلُّه قيمُ أنماطٍ (`space-between`) لا نصوصَ شاشة.

### الاستعداد للنقل

`OPS.md` كاملٌ بالخطوات، وسكربتا النسخ والاستعادة ووحدتا systemd
جاهزة، وفخُّ `promote-admins` مذكورٌ صراحةً. والناقصُ: #١١ و#١٢ و#١٣.
