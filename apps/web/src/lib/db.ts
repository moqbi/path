import "server-only";

/**
 * عميل القاعدة للوحة.
 *
 * العميل نفسه الذي يستعمله الخادم (`packages/db`) لا نسخةٌ ثانية: عميلان
 * في عمليةٍ واحدة يفتحان بِركتَي اتصالٍ على نفس القاعدة. وهذا الملف بابٌ
 * باسمه القديم (`@/lib/db`) فلا يُعاد كتابة كل استيرادٍ في اللوحة
 * المنقولة — وهي تُنقل كما هي.
 */
export { prisma } from "@athar/db";
