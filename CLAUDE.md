# آثار مومنتس · ATHAR Moments — تعليمات المشروع

اقرأ هذا الملف كاملاً قبل أي تعديل. ما فيه من قرارات ليست تفضيلات قابلة
للتفاوض، بل نتيجة تحليل انتهى إليها المشروع بعد مراجعة أسباب فشل Path
و Foursquare و Zenly. تغييرها يحتاج قراراً صريحاً من صاحب المشروع.

## ما هو هذا التطبيق

شبكة اجتماعية حميمية بدائرة محدودة، شعارها «لحظاتك، مع ناسك.»
وريث فكرة Path، لكن مبنية على
الاستخدام الفعلي في الخليج: نشر اللحظات وإعلان الحضور — لا مشاركة الصور.

نموذج أولي على الويب (Next.js). الوجهة النهائية تطبيق جوال على نفس
الخلفية ونفس المخطط.

## قواعد لا تُكسر

هذه القواعد هي المنتج. أي تعديل يخالفها اقترحه على صاحب المشروع أولاً
ولا تنفّذه.

1. **سقف الدائرة ١٥٠ ولا يُباع بأي مبلغ.** بيع خانات أصدقاء يبيع تدمير
   الشيء الوحيد الذي لا تستطيع الشبكات الكبيرة تقليده. كل ما يدخل
   الاشتراك أو المتجر لازم يجتاز هذا الاختبار: **لا تبِع شيئاً يقلّل
   قيمة التطبيق للناس المحيطين بالدافع.** الإطارات والخلفيات والأرشيف
   تجتازه؛ الأصدقاء الإضافيون يرسبون.
2. **لا استكشاف عام إطلاقاً.** لا تبويب Explore، لا اقتراح أشخاص، لا
   محتوى من خارج الدائرة. إضافة الاستكشاف هي بالضبط ما قتل Path.
3. **التفاعلات الخمسة الأساسية مفتوحة للجميع دائماً.** الإيموجي الحر
   وحده خلف الاشتراك. حصر التفاعل الأساسي يقلّل حجم التفاعل الكلي
   ويضرب الاحتفاظ — تيليجرام جرّبها وتراجع.
4. **الإشارة «مع فلان» تظهر فوراً بلا موافقة.** اللحظة تُنشر في صفحة
   كاتبها وحده ولا تدخل صفحة المُشار إليه، وهذا سلوك Path نفسه.
   (المخاطرة المعروفة والمقبولة: دائرة الكاتب تعرف أين كان المُشار إليه
   وإن لم يكن منها.)
5ب. **الكاميرا مدمجة لا كاميرا نظام** (`app/camera.tsx` بـexpo-camera):
   كاميرا النظام تُخرج المستخدم من التطبيق وترجعه بصورةٍ جاهزة — بلا
   معاينةٍ ولا فلاترَ ولا قرارِ «أعِدها». وتستعملها ثلاثٌ: لحظة صورة،
   وقصة صورة، وقصة فيديو (٣٠ ثانية). وصورةُ العرض والغلاف من المعرض
   وحدهما: صورةُ وجهك تُختار لا تُلتقط على عجل.
   والسؤال عند النشر نافذةٌ من الأسفل (`SourceSheet`) لا `ActionSheetIOS`:
   الأخيرة لآبل وحدها فتختلف الشاشة بين الجهازين بلا سبب.
   وإذنُ الكاميرا كان مُعلَناً في `app.json` بلا استعمال — وهذا سببُ ردٍّ
   شائع في مراجعة آبل.
5. **لا يُدخَل شيء يدوياً إذا كان الجهاز يعرفه.**
   المكان من إذن الموقع ثم تحويل الإحداثيات على الخادم — لا كتابة اسم.
   الأغنية من حساب مربوط (سبوتيفاي) — لا كتابة عنوان وفنان.
   والنوم لحظة مفردة بلا نص وبلا «مع مين».
6. **لا صناديق عشوائية في المتجر.** بيع مباشر بسعر واضح. الجمهور صغير
   السن وهذا يجرّ رقابة تنظيمية ويضرب الثقة.
6ب. **التعليقات تُقرأ وتُكتب داخل الخط الزمني.** فتح اللحظة للتفاصيل،
   لا لرؤية سطر تعليق. وقائمة النشر قوس متطاير من زر الزائد
   (`components/composer-fan.tsx`) لا قائمة جافة.
7. **الهوية البصرية: فاتحة، والتدرّج نادر.**
   **الاسم الرسميّ «آثار مومنتس» / «ATHAR Moments»** — كاملاً في
   المتجرين وفي كل موضعٍ يعرّف المنتج: صفحة الهبوط، وشاشة الدخول،
   وبطاقة المشاركة، وعناوين صفحات الويب، و`app.json` → `name`.
   و«آثار / ATHAR» وحدها تبقى في الشريط العلويّ داخل التطبيق
   (`AthrHeaderMark`): شريطٌ بارتفاع ٥٦ لا يتّسع لاسمٍ من كلمتين بجانب
   الرمز وزرّ، والرمز يقول البقيّة. والتفاصيل في `apps/mobile/STORE.md`،
   ومعها المعرّفان اللذان **لا يتغيّران بعد أوّل نشر**
   (`app.athar.mobile` على الجهازين).
   والشعار **صورةٌ لا رسمٌ في الكود**: قمّةٌ بشريطٍ مطويّ ونقطةٌ أعلى
   اليمين، بخلفيةٍ شفّافة في `public/athr-mark.png` و
   `apps/mobile/assets/athr-mark.png` — يقرؤها `AthrMark` في الملفّين
   `brand.tsx` وحدهما، فاستبدال الملف يغيّرها في التطبيق كله بلا لمس
   الكود. وهي في الرؤوس ٤٤ بكسلاً على الويب و٣٢ على الجوّال، وفي شاشة
   الدخول ٩٦ في **أعلى** الشاشة لا أسفلها: الشعار أوّل ما يُرى،
   والخيارات حيث يصل الإبهام.
   الألوان: أرضية المخطط `#EAE5D9` **أغمق من البطاقات** `#FDFCF8` عمداً
   حتى تُقرأ حدود المنشور؛ والأشرطة العلوية والسفلية تبقى داكنة
   `#0E1A24` عبر `.chrome` — لم تكن جزءاً من تفتيح الأرضية.
   كهرماني `#F6B93B`، مرجاني `#FF7A5A`.
   الخطوط: IBM Plex Sans Arabic للمتن، Tajawal للعناوين، Montserrat
   للاتيني (`.latin`). مقاس المتن ١٥px وارتفاع السطر ١٫٧٥ — العربية
   تحتاج تنفّساً أكبر من اللاتينية.
   الدلالات ثابتة: **المرجاني للحالات الحية**، **الكهرماني لآثار+**،
   **التدرّج للشعار والفعل الأساسي فقط** عبر `.brand-gradient`.
   الكهرماني الأصلي لا يُقرأ نصاً على فاتح — استعمل `--color-clay-ink`.
   الأيقونات SVG بحدّ على شبكة ٢٤ بكسل. أما **التفاعلات فملفات في
   `public/reactions/*.png`** من رسوم المالك: استبدال الملف يغيّر شكله في
   التطبيق كله بلا لمس الكود. ومع كل تفاعل تظهر **صورة صاحبه** لا الرقم
   وحده — سؤال الدائرة الصغيرة الأول هو «مَن».
8. **التفاعل لا يفتح اللحظة.** زرّ واحد في **يسار المنشور**
   (`components/moment-bar.tsx`)، وبالضغط تُفتح الوجوه ومساحة التعليق
   معاً — ولا يحمل كل منشور حقل كتابة مفتوحاً وهو في الغالب لا يُستعمل.
   والزر يوقف الحدث قبل أن يصعد إلى الرابط.
   وأي عنصر عائم يُثبَّت بـ`.shell-fixed` لا بـ`fixed` وحدها، وإلا طار
   خارج هيكل الهاتف على المتصفح العريض. وزوايا قوس النشر تبقى ≤ ٩٠°
   لأن الزر على اليمين، وأي زاوية أكبر تدفع الصنف خارج الشاشة.
10. **الهيكل بارتفاع الشاشة** (`.screen`) والتمرير في منطقة واحدة
   (`.scroll-area`)، فالشريطان العلوي والسفلي ثابتان دائماً. مع
   `sticky` كان السفلي يختفي حتى ينزل المستخدم.
11. **كل حاوية `flex-grow` تأخذ `min-w-0`.** بدونها لا تنكمش تحت عرض
   محتواها، فنصٌّ لاتيني طويل بلا فواصل يوسّع الصفحة أفقياً — وفي RTL
   ينزلق العرض إلى الحافة اليمنى فيبدو التطبيق كله منزاحاً. حدث فعلاً
   مع عنوان أغنية، و`.shell` فيه `overflow-x: hidden` كحارس أخير.
12. **الصور تُخزَّن في القاعدة** (`Media`) وتُقدَّم من `/api/media/[id]`،
   ويصغّرها المتصفح قبل الرفع. قرار مرحلة يتجنّب خدمة تخزين ومفاتيحها؛
   الانتقال إلى S3 لاحقاً يمسّ هذا الجدول وحده لأن كل شيء يشير إلى
   معرّف لا إلى مسار.
13. **لوحة التحكم** في `/admin` للمشرفين (`role = ADMIN`)، وكل إجراء
   فيها يفحص الدور بنفسه — إخفاء الرابط ليس حماية.
   ومكانها الآن `apps/web/src/app/(admin)/` بإجراءاتها وحدها في
   `apps/web/src/app/actions.ts`؛ والنسخة في `src/app/admin/` تبقى تعمل
   حتى يتقاعد الويب الحالي. وفيها قسما **البلاغات** و**الكلمات الممنوعة**:
   البلاغ يُحسم بيد — «حُذف» يمسح المحتوى ويُبقي صفَّ البلاغ سجلّاً فيبقى
   تكرار المخالفة مرئياً — والقائمة يُفحص بها كل ما يُكتب
   (`lib/moderation.ts`) في الخادم والويب معاً. والدور يُضبط عند
   الإقلاع بـ`scripts/promote-admins.ts` من `ADMIN_EMAILS`: البذرة
   محروسة بقاعدة فارغة، فنشرٌ على قاعدة عامرة يترك الجميع بلا دور،
   وهذا ما عطّل اللوحة على Render.
14. **رأس الخط الزمني** (`components/timeline-head.tsx`): صورة العرض
   **داخل** الغلاف كاملةً على محور خط المخطط، ومن أسفلها ينزل الخط إلى
   أول لحظة. الساعة إلى جانبها وزر التحديث في الطرف المقابل.
   السحب من الأعلى يمدّ الغلاف وحده — الشريط العلوي لا يتحرّك — وبالإفلات
   يُعاد جلب الصفحة. **اللمس يحتاج مستمعات أصلية `passive: false`**:
   بأحداث React وحدها يبتلع المتصفح الإيماءة ويُلغي `pointer` فلا يتمدّد
   شيء تحت الإصبع — يعمل بالفأرة ولا يعمل على الجوال. والساعة تُحسب بعد
   التركيب لا على الخادم وإلا اختلف النصّان عند الترطيب.
15. **رقم العضوية** (`User.memberNo`) تسلسل من ١ فصاعداً، ومصدره متسلسلة
   Postgres لا حساب في التطبيق — وإلا تصادم مسجّلان في اللحظة نفسها.
   لا يُعاد استعماله ولا يتغيّر، ويظهر في الملف الشخصي وقائمة الأصدقاء.
16. **الوسوم** (`Tag`): كلمة بجانب الاسم بلونَي خلفية ونصّ يختارهما
   المشرف. وسمٌ واحد `autoForPlus` يُقرأ لكل مشترك في آثار+ ولا يُكتب على
   حسابه — الاشتراك ينتهي من نفسه، والوسم المكتوب كان سيبقى بعده.
   العرض عبر `TagPill` و`tagOf` وحدهما، فلا يختلف شكله بين شاشة وأخرى.
   وعلى الجوّال `components/name-tag.tsx` — نسخته حرفاً بحرف. وكان
   الوسم غائباً عن الجوّال كلّه والخادم يرسله منذ بُني: الخط الزمني
   و«أنا» كانا يرسمانه بأيديهما فبقي في غيرهما لا شيء. الآن في
   التعليقات والأصدقاء والمقترحين والمحادثات وملف الصديق أيضاً.
17. **خلفية الغلاف من `coverStyle()` وحدها.** لا تُخلط `background`
   المختصرة مع `background-size`/`background-image` في نفس العنصر: React
   يحذف أحدهما عند إعادة الرسم فتختفي الصورة بلا خطأ يظهر. وكل زر يطفو
   على الغلاف يُركَّب **داخله**، لا داخل `.scroll-area` بإزاحة سالبة —
   منطقة التمرير تقصّ ما خرج عنها، وهكذا اختفى زر تغيير الغلاف.
18. **الأحداث أسطر لا بطاقات.** الانتقال إلى مدينة، النوم، المكان،
   الصديق الجديد، الأغنية — كلها سطرٌ على ورق الخط الزمني ومعه أيقونة
   الحدث، وما يجتمع حولها من تفاعل وتعليق يجلس في قالبٍ أبيض تحته.
   البطاقة تبقى لما له متن: صورة أو خاطرة.
19. **شريط التفاعل ينطوي بنفسه**: باختيار وجه، أو بإرسال تعليق، أو بضغطة
   خارجه ما لم يكن هناك تعليق نصف مكتوب. صندوق مفتوح على كل منشور ضجيج.
20. **الإضافة من أصدقاء الأصدقاء وحدهم.** لا بحث بالاسم ولا بالبريد:
   `suggestions()` تعرض من يجمعك به صديق مشترك، و`requestFriend` ترفض من
   لا يجمعك به أحد. ومن ليس في دائرتك تُعرض بطاقته وحدها — لا لحظاته ولا
   محادثته — والخادم يمنع الاثنين لا الواجهة وحدها.
21. **حذف الحساب داخل التطبيق** (`me/delete.tsx`) ومن الموقع
   (`apps/web` ← `/delete-account`، بالبريد وكلمة المرور) — شرط متجر
   آبل لكل تطبيق فيه تسجيل دخول، وجوجل بلاي تطلب طريقاً إليه من خارج
   التطبيق يبلغه من حذفه من جهازه، ولا يكفي فيه تعطيل ولا مراسلة دعم. حذفٌ حقيقي:
   صفّ المستخدم يذهب ومعه كل ما يشير إليه بالتتالي. وكلمة المرور شرط،
   والخطأ فيها يُردّ رسالةً في الشاشة لا استثناءً — هذه آخر خطوة قبل فقد
   كل شيء.
22. **التصنيفات يملكها صاحبها** (`FriendGroup`): تصنيفك لشخصٍ «عائلة» لا
   يراه هو ولا غيرك. وكل حدٍّ في الخصوصية يُقاس بتصنيف: من يرى لحظاتي،
   ومن يتفاعل معي.
23. **جمهور اللحظة يُفحص في الاستعلام** (`lib/visibility.ts`): الدائرة
   كلها، أو تصنيف، أو أشخاص بأعيانهم — و`visibleWhere()` هي الباب الوحيد
   لقراءة اللحظات. أي استعلام جديد يمرّ منها وإلا سرّبت لحظةً خاصة.
23ب. **معرّف الملف ليس صلاحية** (`canSeeMedia`): باب `/api/media/[id]`
   و`/v1/media/:id` يفحص الجلسة ثم من يرى ما عُلِّق عليه الملف — لحظةٌ
   بقواعد جمهورها، وقصةٌ لدائرة صاحبها، ورسالةٌ لطرفَي محادثتها، وصورةُ
   عرضٍ لمن لا يحجبه حظر، وصنفُ متجرٍ للجميع. وكان الباب مفتوحاً بلا
   حساب أصلاً وبترويسة `public` تُجيز لوسيطٍ مشترك أن يحتفظ بصورة أحدهم
   ويقدّمها لغيره. والردّ «غير موجود» لا «ممنوع».
23ج. **صورةُ العرض والغلاف عامّتان لصفحة المشاركة** — استثناءٌ مقصود
   للقاعدة ٢٣ب وحدودُه مرسومة: `/api/public/[avatar|cover]/[memberNo]`
   لا يقبل معرّف ملفٍ أصلاً بل **رقم عضوية**، ويقرأ منه حقلين اثنين لا
   غير. فلا يُفتح به ملفُّ لحظةٍ ولا قصةٍ ولا رسالة مهما عُرف معرّفه.
   وخبيئتُه `public` صحيحة لا خطأ: زاحفُ واتساب وتويتر يقرأ الصورة بلا
   كوكي، فلا سبيل إلى «خاصّة» — ومن أعطى رابطه أعطى وجهه.
24. **الحظر في الاتجاهين** ويُفكّ الصداقة. شرط متجر آبل، وحظرٌ من طرف
   واحد يترك المحظور يقرأ ويعلّق فليس حظراً.
25. **الإشعارات تُشتقّ ولا تُخزَّن** (`lib/notifications.ts`): جدولٌ ثالث
   يعني كتابةً عند كل تفاعل ومسحاً عند كل حذف، والاشتقاق يبقيها صادقة.
26. **الضغطة المطوّلة على «اللحظات»** تفتح اللحظات الخاصة وآثارنا — بابٌ
   يُقصد قصداً ولا يستحق تبويبين دائمين.
   والشريط السفلي خمسة: اللحظات، الأصدقاء، الإشعارات، المتجر، أنا؛
   وعدد الجديد يُجلب في `components/tab-bar.tsx` مرة لكل صفحة لا في
   كل شاشة على حدة.
27. **رأسا «اللحظات» و«أنا» ثابتان**: الغلاف والصورة والبيانات خارج
   `.scroll-area`، واللحظات وحدها تمرّ تحتها — وإلا تداخلت اللحظات مع
   الغلاف عند النزول. والسحب يمدّ الغلاف الثابت نفسه.
   ولا وضع ليلي: جُرّب وأُلغي بطلب المالك.
   ورأس «أنا» ينكمش مع التمرير (`me/shell.tsx`): النبذة والإحصاءات تُطوى
   أولاً — تُقرأ مرة وتُعرف — ويبقى الاسم والصورة وزرّ التعديل. والقيم
   تتبع الإصبع بلا انتقال زمني، وإلا لاحقت الحركةُ نفسها فأُحسّت مطّاطية.
28. **`preventDefault` على حاوية لا يوقف الصعود وحده**: يبتلع إرسال
   النموذج داخلها. حاوية `MomentBar` توقف الصعود فقط
   (`stopPropagation`)، و`preventDefault` للأزرار التي تجلس داخل رابط.
29. **الشريط السفلي فاتح** (`.tabbar`) والرأس داكن (`.chrome`):
   التبويبات جزء من الصفحة يُلمس كثيراً، والرأس يحمل العلامة.
   والضغطة المطوّلة تُطيّر بابيها بحركة قوس النشر نفسها.
30. **صور اللحظات تُعرض بنسبتها** (`components/photo.tsx`) من أبعادها
   المحفوظة، والطويلة جداً وحدها تُقصّ. والضغط عليها يفتحها كاملة.
31. **نماذج اللوحة تُرجع رسالة لا استثناء** (`admin/saver.tsx`):
   استثناءٌ في إجراء خادم يطيّر الشاشة إلى صفحة خطأ عامة، فلا يعرف
   المشرف أين أخطأ ولا إن كان قد حُفظ شيء.
30. **القصص** (`lib/stories.ts`): صورة تُعرض لأصدقائك يوماً ثم تذهب.
   لا تدخل الخط الزمني ولا الأرشيف — هذا الفرق بينها وبين اللحظة.
   والانتهاء يُقرأ من `expiresAt` في الاستعلام لا من مهمة تنظيف، فتختفي
   في لحظتها. الحلقة الملوّنة تعني «فيها ما لم تره».
31. **الحضور** (`User.lastSeenAt`) يُختم في `currentUser()` مرة كل دقيقة
   لا مع كل طلب، والفشل يُبتلع — الحضور زينة لا تُعطّل صفحة.
32. **اللحظات الخاصة وآثارنا عدساتٌ في الخط الزمني** (`/?view=…`) لا
   صفحات: الرأس والغلاف والصورة تبقى، ويتبدّل ما تحتها وحده. واسم تبويب
   «اللحظات» يقول أي عدسة مفتوحة، والضغطة المطوّلة تعرض العدستين
   الأخريين لا الثلاث — عرضُ ما أنت فيه خيارٌ لا يفعل شيئاً. ولا شرائح
   تحت الغلاف ولا زرّ رجوع. والصفحات القديمة تتحوّل إلى العدسة فلا يكسر
   رابط.
33. **لا عدّاد مشاهدات في اللحظة**: أُزيل بطلب المالك — يأخذ مساحة ولا
   يقول شيئاً. المشاهدات ما زالت تُسجَّل، وتُعرض في صفحة اللحظة وحدها.
34. **إيصال الرسالة ثلاث حالات** (`components/receipt.tsx`): صحٌّ واحد
   إذا خرجت، وصحّان إذا وصلت (`deliveredAt`)، ويتلوّنان بلون العلامة إذا
   قُرئت (`readAt`). والتسليم يُكتب مع ختم الحضور و عند فتح المحادثات —
   لا خادم دائم بيننا، فحضوره على الشاشة هو دليل الوصول.
35. **الرسالة تُعدَّل لصاحبها وحده** (`editMessage`)، ويبقى «معدّلة»
   ظاهراً للطرفين — لا نُخفي أنّ الكلام تغيّر بعد قراءته.
36ب. **وعلى الجوّال الموجةُ نفسها مادّةً** (`assets/tap.wav`): لا
   WebAudio في React Native، فوُلِّدت مرّةً (٨٨٠ هرتز تخفت في عُشر
   ثانية) وتُحمَّل مرّةً وتُعاد من مكانها — إنشاءٌ مع كل ضغطة يتأخّر عن
   الإصبع فيُسمع بعد الفعل لا معه.
36. **الصوت نغمة مركَّبة لا ملف** (`lib/sound.ts`): WebAudio يُنشأ عند
   أوّل لمسة لا قبلها، وموجة واحدة تخفت في عُشر ثانية. وإذا طلب الجهاز
   «قلّل الحركة» سكتنا — من يطلب هدوء الحركة يطلب هدوء الصوت معه.
   يُسمع عند إفلات السحب للتحديث، وعند فتح علامة الزائد وإغلاقها.
37. **التعليق في فقاعته** (`components/comments.tsx`): صورة صاحبه رابطٌ
   إلى ملفه، ثم اسمه ووقته، ثم نصّه على أرضية الورق. سطرٌ عارٍ يخلط
   الاسم بالنص فلا يُعرف أين ينتهي تعليقٌ ويبدأ آخر.
38. **الحظر من صفّ الصديق لا من ملفه**: السحب يكشف «حظر» و«إزالة» معاً
   (`SwipeRow` بفعلٍ ثانٍ). الملف يُقرأ، وأدوات القطع تُجمع في مكان
   واحد — وشرط آبل محفوظ: الحظر موجود لا محذوف.
39. **الإهداء** (`giftItem`): شراءٌ يدفعه واحد ويملكه آخر — للأصدقاء
   وحدهم، ولا يُهدى ما يُكتسب بالوقت، ولا صنفُ «آثار+» لمن ليس مشتركاً،
   ولا ما يملكه أصلاً. الخصم بسعر المُهدي، والتمليك والخصم في معاملة
   واحدة. والإشعار مشتقٌّ من `Purchase.giftedById` لا مكتوبٌ في جدول.
40. **التحرير بابٌ واحد داخل التبويب** (`me/edit-sheet.tsx`): الغلاف
   (تغييره وضبطه وإزالته) وصورة العرض والبيانات في نافذةٍ تُفتح من زرّ
   «تعديل الملف». لا أيقونةَ تحرير على الملف نفسه — يُقرأ نظيفاً — ولا
   صفحة مستقلّة: `/me/edit` تحويلٌ إلى `/me` فلا ينكسر رابطٌ قديم.
   و`saveProfile` تردّ نتيجةً ولا تُعيد التوجيه، فالنافذة تُغلق نفسها.
41. **الإكسسوارات تُلبَس من الملف** (`me/accessories.tsx`): المتجر يبيع
   والملف يُلبِس، فيعرف صاحبه أين يجد ما اشتراه أو ما أُهدي إليه.
   والهدية تحمل اسم مُهديها في اللوحة.
42ب. **ملف الصديق يلبس الخطّ الزمني نفسه**: غلافٌ بـ`CoverLayer`
   وحشوةٌ جانبية ٢٠ للحظات. صورةٌ عاريةٌ في مربّعٍ رماديّ وبطاقاتٌ بلا
   حشوة تُقرأ صفحةً مكسورة من تطبيقٍ آخر.
42. **ملف الصديق ليس صفحةً فرعية**: الشريط السفلي يبقى تحته ولا زرّ
   رجوع فوقه — التبويب يؤدّي الغرض، والزرّان معاً تكرار.
   وعلى الجوّال هذا **مكانُ الملفّ في الشجرة** لا نمطٌ يُكتب:
   `app/(tabs)/u/[id].tsx` بـ`href: null` — شاشةٌ داخل المكدّس السفلي
   بلا تبويبٍ يخصّها. وكان في `app/u/[id].tsx` خارج المجموعة، فالشريط
   يختفي تحته مهما كُتب في الشاشة. و«الأصدقاء» يبقى مضيئاً تحته
   (`usePathname`) كما في الويب: من هناك يُفتح.
43. **زرٌّ واحد لكلّ فعل في الملف**: الخصوصية والخروج بجانب
   «إكسسواراتي»، والغلاف لا يحمل إلا اختيار الصورة. لا نسخة ثانية من
   زرٍّ في زاوية أخرى.
44. **المتجر يكبر من اللوحة** (`StoreCategory`): الشرائح تصنيفاتٌ في
   القاعدة تُضاف وتُرتّب وتُخفى بلا نشر نسخة. و«المميز» ليس تصنيفاً بل
   واجهة: «وصل حديثاً» من `createdAt`، و«ثيمات آثار» من نوع الصنف،
   و«حزم محدودة» من `limited` — صفوفٌ مشتقّة لا مرصوفة يدوياً.
   وحذف التصنيف لا يحذف أصنافه: تعود بلا تصنيف فلا يضيع ما اشتراه أحد.
45. **التميمة تُلبَس كالإطار** (`User.charmId`)، وتُهدى وتُنزع مثله —
   وما كان مكتوباً هنا من «تُملَك ولا تُلبَس» نُسخ منذ بُنيت. والمتجر
   والإكسسوارات يقولان «قريباً». لا حقلٌ يُكتب على أمل ميزة قادمة.
46. **لا وضعَ ليلي**: أُلغي بطلب المالك، وحُذفت أنماطه من
   `globals.css` — لم تبقَ `[data-theme="dark"]` معلّقةً بلا مفتاح
   يشغّلها. والثيم هو الذي يغيّر مزاج التطبيق اليوم.
47. **الثيم صورةٌ تعمّ التطبيق**: تُرفع من اللوحة وتُخزَّن كبقية الصور
   (`StoreItem.mediaId` → `/api/media`)، ومن يلبسها يراها خلف اللحظات
   والملف والأصدقاء والإشعارات والرسائل — تُلبَس مرّةً في `layout.tsx`
   على `.shell`، وفوقها حجابٌ فاتح (٧٠٪) يُبقي النصّ مقروءاً بلا أن يطمس
   الصورة. وما يقع على الورق مباشرة — سطور الأحداث وعناوين الأيام
   والساعات وزرّ التفاعل — يُكتب بوزنٍ أثقل وحبرٍ أغمق وأرضيةٍ صلبة، فما
   يُقرأ على ورقٍ ساكن قد يذوب فوق صورة. ولذلك
   `currentUser()` ملفوفةٌ بـ`cache`: التخطيط والصفحة يقرآنها معاً.
47. **التميمة شعارٌ حرّ أسفل يسار صورة العرض** (`Avatar charm`) أينما
   ظهرت الصورة: ٠٫٦٨ من قطر الصورة، بلا حلقةٍ ولا قصٍّ في قرص
   (`contain`)، وظلٌّ خفيف يفصلها. وتُرفع PNG بشفافيتها — ضغطها JPEG
   يُلبسها مربّعاً أسود. واليسار مقعدها في كل مكان، ولذلك انتقل زرّ
   تغيير الصورة في ملفي إلى اليمين. تُستثنى دائرتان يشغل ركنُهما شيءٌ
   آخر: المتفاعلون (وجه التفاعل) وصفوف الإشعارات (شارة النوع).
48. **التعليق بلا أرضية**: ترتيبٌ (صورة، ثم اسمٌ ووقت، ثم نصّ) بلا لونٍ
   تحته — الطبقة الملوّنة تُقرأ ظلاماً فوق البطاقة، والترتيب وحده يكفي.
49. **لوحة المتجر بابان**: «الأصناف» و«التصنيفات» (`?s=store&v=…`).
   نماذج الإضافة مطويّة، والأصناف مرصوفة تحت تصنيفاتها كما تُرى في
   المتجر، وكلّ حقلٍ يحمل عنوانه — صفٌّ من حقولٍ عارية لا يقول ما يُكتب
   فيه.
50. **لا خلط بين `background` المختصر ولواحقه** في كائن نمطٍ واحد
   (`itemPaint` في `ui.tsx`): الخلط يجعل React يحذّر ويترك بقايا خلفيةٍ
   سابقة بعد إعادة الرسم.
51. **رسوم قائمة النشر في `public/composer`** (كرسوم التفاعلات): صورٌ
   ملوّنة لا خطوطٌ رمادية، تُستبدل بتبديل الملف بلا لمس الكود. وتباعُدها
   محسوب: المسافة على القوس = نصف القطر × الزاوية بالراديان
   (٢٠٥ × ٢١° ≈ ٧٥ بكسل بين مركزين، والقرص ٥٦). وأعلى الأصناف عند ٨٨°
   لا ٩٠ — القرص عند القائمة تماماً يخرج من حافة الإطار.
52. **رؤوس الشاشات بوزنٍ واحد**: `chrome … px-5 pb-3 pt-4` وأقراص
   أيقوناتها ٤٠ بكسلاً — قرصٌ ٤٤ في رأسٍ واحد يرفعه عن جيرانه فيُقرأ
   «أكبر» بلا سبب ظاهر. وشاشةٌ لها اسمها الخاص تحمل `AthrPageMark`:
   الرمز، ثم خطٌّ فاصل، ثم الاسم — لا اسمٌ عارٍ بلا علامة.
53. **الغلاف يذوب في أرضيته** (`CoverFade`): تحته قد تكون صورة ثيمٍ
   أخرى، والتقاؤهما بحدٍّ حادّ يُقرأ صورتين مرصوفتين بالغلط.
54. **«صحيت» لحظةٌ ساعتُها خبرها** (`MomentKind.WAKE`): لا نصّ يُحفظ —
   `createdAt` يحمل الوقت والعرض يقرأه، فلا يتجمّد سطرٌ مكتوب حين يتغيّر
   تنسيق الساعة. والنوم والصحو طرفا اليوم فيتجاوران في طرف قوس النشر.
55. **زوايا قوس النشر تُقسَّم على عدد الأصناف** لا تُكتب لكل صنف: القوس
   من ٨٨° إلى ٤°، والمسافة بين مركزين = نصف القطر × الزاوية بالراديان
   (٢٤٦ × ١٦٫٨° ≈ ٧٢ والقرص ٥٦). صنفٌ سادس بزوايا ثابتة كان سيُلصق
   الأقراص أو يُخرج أعلاها عن الشاشة.
57. **الغلاف يُقنَّع لا يُدهَن** (`CoverLayer`): الذوبان قناعٌ
   (`mask-image`) على صورة الغلاف نفسها. دهنُ تدرّجٍ بلون الورق فوقها
   كان يترك شريطاً رمادياً يُرى — الورق لونٌ مصمت والثيم تحته صورة.
58. **الاشتراك نجمةٌ لا وسم** (`NameTag`): الوسم التلقائي «داعم» انتهى،
   وبقي وسمٌ يُمنح من اللوحة لشخصٍ بعينه. والنجمة أصغر ولا تحتاج ترجمةً
   حين يكون الاسم لاتينياً — ولذلك كل اسمٍ يُكتب بـ`dir="auto"`: يبقى
   ترتيب الصفّ عربياً ويُقرأ الاسم باتجاهه هو. ووسم صاحب التعليق يظهر
   في التعليقات أيضاً.
59. **التميمة على حافة الصورة**: مركزها على المحيط تماماً، فنصفها داخل
   الصورة ونصفها خارجها.
60. **رأس كل شاشةٍ لها اسمها يحمل العلامة**: اللحظة والمتجر و«أنا» —
   الرمز، ثم فاصل، ثم الاسم. واللحظة المصوّرة لم تعد تُخفي رأسها وتضع
   سهماً عائماً فوق الصورة.
61. **صاحب اللحظة يحذفها من لوحة التفاعل** (`deleteMoment`): سؤالٌ ثم
   حذف. والتفاعلات والتعليقات تذهب معها بـ`Cascade`.
62. **صورة الأغنية هي زرّ تشغيلها**: زرٌّ ثالثٌ بجانبها كان يزاحم زرّ
   التفاعل في الطرف نفسه.
63. **المكان يختاره صاحبه من حوله** (`nearbyPlaces` عبر Photon): أقرب
   عنوان يردّ شارعاً، والشارع لا يقول أين أنت — في المقهى أم في المطعم
   المجاور. وOverpass مُهمَل هنا: خوادمه العامة تمهل ثم تردّ ٥٠٤.
   والفشل يردّ قائمةً فارغة فيُكتب أقرب عنوان ولا يتعطّل النشر.
65. **شاشة النشر رأسها «لحظة» بالعلامة**، لا اسم النوع عارياً: النوع
   يُقرأ من الشاشة نفسها — منتقي صورة، أو حقل رابط، أو قائمة أماكن.
65ب. **١٠٠ حرفٍ حدّ النبذة** (`BIO_MAX`): سطرٌ يُقرأ تحت الاسم لا فقرةٌ
   تُقرأ — وكان ١٦٠ فيدفع الاسمَ والإحصاءات إلى أسفل الشاشة في رأسٍ
   ينكمش أصلاً. وفي الشاشة والخادم معاً كحدّ اللحظة، ومعه عدّادُ ما
   بقي في طرف السطر.
66. **٢٥٠ حرفاً حدّ اللحظة** (`TEXT_MAX` في الشاشة وفي `postSimple`):
   ما زاد صار مقالاً. والحدّ في المكانين، فلا يقصّ الخادم ما سمحت به
   الشاشة.
67. **الأشخاص يُختارون في نافذةٍ ببحث** (`PeopleSheet`)، لا جدارَ أزرار:
   عند مئةٍ وخمسين صديقاً كان الجدار يبتلع الشاشة قبل زرّ النشر. وهذا
   يسري على «مع مين؟» و«أشخاص أختارهم» معاً.
68. **الموقع اختياريٌّ على اللحظة والصورة** (`readPlace`): زرٌّ يُضغط
   فيُطلب الإذن — إذنٌ يُطلب بلا سبب يُرفض بلا تفكير. وقواعده قواعد لحظة
   المكان: «إظهار موقعي» مطفأً يُبقي المدينة وحدها.
69. **صفحة اللحظة قالبٌ واحد بخطّين**: المنشور، خط، التفاعلات، خط،
   التعليقات. سطورٌ سائبة فوق قالب تعليقاتٍ وحده تُقرأ كأن التعليقات هي
   الموضوع.
101. **الملفات في Cloudflare R2 لا في القاعدة** (`storage.ts` ← `r2.ts`):
   قاعدةٌ تحمل الفيديو تكبر بلا سقف، ونسخُها الاحتياطي يصير نسخاً
   للملفات. وR2 بلا رسوم خروج. والاتصال بتوقيع SigV4 مباشرةً لا بحزمة
   AWS — ثلاثة أفعال فقط، والتوقيع مقيسٌ على أمثلة AWS المنشورة.
102. **بلا مفاتيح يعمل كل شيء على القاعدة**: بيئةٌ لم تُربط بعد لا يتعطّل
   فيها الرفع. و`R2_ENDPOINT` يفتح الباب لأيّ تخزينٍ متوافق مع S3 — وهذا
   يهمّ لو لزم أن تبقى الملفات داخل المملكة.
103. **البثّ يمرّ بالتطبيق لا برابطٍ مباشر**: الدلو مغلق، فلا رابط يُنسخ
   ويُوزَّع خارج الدائرة، والصلاحية تبقى حيث تُفحص. ومعه طلب المدى
   يُمرَّر كما هو.
104. **الحذف يشمل الكائن** (`dropMedia`): حذفُ الصفّ وحده يُبقي البكسلات
   في السحابة إلى الأبد بلا شيء يدلّ عليها. يسري على القصة المنتهية
   واللحظة المحذوفة والحساب المحذوف.
95. **الصوت يقف عند حدّه وهو يُسجَّل** (`Composer`): عشرون ثانية للجميع
   ومئة وعشرون لمشتركي آثار+. الوقوف في المسجّل نفسه لا بعد الإرسال —
   ومن يكتشف بعد دقيقتين أنّ تسجيله لن يُقبل لن يسجّل ثانيةً. والحدّ
   يُعاد فحصه على الخادم (`VOICE_SECONDS`).
96. **القصة تُراجَع قبل نشرها** (`StoryComposer`): اختيارٌ ثم معاينةٌ
   بفلتر ثم نشر. رفعٌ فوريٌّ بمجرّد اختيار الملف يُنشر ما لم يُرَ.
97. **الفلتر يُحفظ اسماً لا يُحرق في الصورة**: الصورة تبقى كما صُوِّرت
   ويُطبَّق الفلتر عند العرض — فالتراجع ممكن، والفيديو لا يُعاد ترميزه
   في المتصفح أصلاً.
98. **فيديو القصة ثلاثون ثانية وتسعة ميغا**، ومدّته تُقرأ من الملف نفسه —
   وملفات `MediaRecorder` تخرج أحياناً بمدّةٍ `Infinity` فتُقاس بالقفز
   إلى زمنٍ بعيد ثم العودة.
99. **`/api/media` يردّ ٢٠٦ لطلب المدى**: سفاري لا يشغّل فيديو من مصدرٍ
   يردّ الملف كاملاً بـ٢٠٠ على طلب `Range`.
100. **القصة تُحذف هي وملفها بعد يوم** (`sweepStories`): العلاقة من جهة
   الملف، فحذف القصة وحدها يُبقي بكسلاتها. ومع كنس المحادثات يجري مع
   نبض الحضور، لا بمهمّةٍ مجدولة على خادمٍ لا نملكه.
94. **الصور تُرفع ملفاً في `FormData` لا نصّاً في وسيط** (`storeUpload`):
   وسائط إجراءات الخادم محدودةٌ بمليون «فتحة»، ونصّ data URL لصورةٍ
   متوسطة يتجاوزها — فيسقط الرفع بخطأ React مُصغَّر لا معنى له. والملف
   يمرّ جزءاً مستقلاً: بلا ترميز base64 يكبّره الثلث، وبلا عدٍّ.
   و`serverActions.bodySizeLimit` مرفوعٌ إلى ٦ ميغا لأن الحدّ الافتراضي
   ميغابايت واحد.
86. **رأس كل شاشةٍ: العلامة ثابتة ثم اسم الشاشة** — الأصدقاء والإشعارات
   والملف الشخصي وملف الصديق. اسمٌ يحلّ محلّ العلامة يجعل الشاشة تبدو
   خارج التطبيق.
87ب. **صفحة المشاركة `(public)/u/[memberNo]`**: بطاقةُ شخصٍ لمن لم
   ينزّل التطبيق — صورةٌ واسمٌ ونبذةٌ ورقمُ عضويةٍ ومدّةُ بقاء، ثم دعوةٌ
   إلى التحميل. **ولا لحظات فيها ولا أصدقاء ولا عددُهم**: عددُ الدائرة
   يقول عن صاحبها ما لم يأذن بقوله. و`noindex` فلا تدخل نتائج البحث —
   لا اكتشاف عام (القاعدة ٢)، والصفحة تُفتح برابطٍ أعطاه صاحبها.
   والرقم غير الموجود «غير موجود» لا «ممنوع».
87. **رابط الملف يُشارَك برقم العضوية** (`ShareProfile`): لا اكتشاف عام،
   فالرابط ما تعطيه من تعرفه. ومشاركة النظام أولاً، والنسخ حين لا توجد —
   ويُقال أيّهما حدث.
88. **حذف الحساب في الخصوصية** لا في أسفل الملف: هناك يُبحث عنه.
   **وبابُ لوحة التحكم معه** للمشرف وحده: الخصوصية درج الإعدادات، ولا
   تستحق اللوحةُ تبويباً دائماً يراه الجميع ولا يفتحه أحد. وتُفتح في
   المتصفّح على `SITE_URL/admin`، والدرجة تُفحص هناك لا هنا.
89. **لا تقس صورةً بـ`createImageBitmap`**: لا يفكّ الـGIF في كل
   المتصفحات، فيرمي خطأً يُقرأ «الصورة كبيرة» وهي ليست كبيرة. العنصر
   `img` يقيس كل صيغة. وشروط الصورة المتحركة مكتوبةٌ للمستخدم (`ANIMATED`)
   ومفحوصةٌ بنفس الأرقام على الخادم.
90. **الإجراء يردّ نصّ الخطأ ولا يرميه** حين يُستدعى من زرٍّ لا من نموذج:
   الرمي يُسقط الشاشة كلها، فيرى صاحبها فشلاً بلا سبب — أو لا يرى شيئاً.
91. **الهدية سطرٌ عند الطرفين** (`GIFT_SENT` و`GIFT_GOT`): كالصداقة
   تماماً. الصنف في `text`، والطرف الآخر إشارةٌ فيبقى اسمه حيّاً.
92. **النوافذ تُرسم على جسد الصفحة** (`createPortal`): `fixed` داخل عنصرٍ
   عليه `transform` يُقاس من ذلك العنصر لا من الشاشة — وصورة العرض في
   الملف داخل صندوقٍ يتقلّص بالتمرير، فكانت النافذة تُحبس خلف شريط
   التبويبات.
93. **ما رآه المستخدم يُشترى من مكانه**: بطاقة الإطار أو التميمة فيها
   سعرها وزرّ شرائها — لا «افتحه في المتجر» ليبحث عمّا رآه قبل لحظة.
77. **الرجوع يقرأ الطريق لا الصفحة** (`useGoBack`): وجهةٌ مكتوبة في
   الشاشة تُرجع من محادثةٍ فُتحت من الأصدقاء إلى المحادثات. التاريخ يعرف
   من أين جئت، والوجهة المكتوبة تبقى للفتح المباشر وتحديث الصفحة.
78. **السحب من الحافة رجوع** (`BackSwipe`): من الحافتين معاً — RTL تُتوقَّع
   من اليمين، ومن تعوّد اللاتينية يسحب من اليسار. والسحبة تبدأ من الحافة
   نفسها: ما بدأ من الوسط يخصّ القائمة تحته. والنوافذ تُغلق بسحبها إلى
   أسفل (`useSwipeDown`) — وهو «رجوعها».
79. **لا تلتقط المؤشر قبل أن يثبت السحب أفقياً** (`SwipeRow`): التقاطه في
   `pointerdown` يحوّل الضغطة كلها إلى الصفّ، فلا يصل النقر إلى الرابط
   ولا إلى الزرّ داخله — وبه كانت صفوف الأصدقاء والمحادثات لا تُفتح
   بالضغط أصلاً.
80. **لا حلقة بيضاء تحت الإطار**: الإطار يلامس الصورة مباشرة. الحلقة
   بينهما تجعلهما يُقرآن قرصين لا إطاراً على وجه.
   و**الإطار يُمرَّر كاملاً لا `spec` وحده** (`Frame` في `ui.tsx` و
   `components/avatar.tsx`): كان `Avatar` يقبل `frameSpec`، فصورةُ
   الإطار المرفوعة تُهمَل بلا خطأٍ يظهر ويُرسم التدرّج مكانها — على
   الويب والجوّال معاً، بينما المتجر يرسمها فتختلف البطاقة عن الوجه.
   والاستعلام يقرأ `mediaId` مع `spec`: `select` الضيّق يُسقط الحقل
   صامتاً. **ويُحفظ PNG بشفافيته كالتميمة**: ضغطه JPEG يُلبسه مربّعاً
   مصمتاً يُغطّي الصورة.
81. **صورة العرض بابٌ لما يلبسه صاحبها**: ضغطُها يعرض الصورة، أو بطاقة
   التميمة، أو بطاقة الإطار — وما لا يلبسه لا يُعرض سطراً فارغاً.
82. **الصورة المتحركة تُرفع بملفها** (`animated` في `ImagePicker`):
   `canvas` يرسم الإطار الأول وحده فيقتل الحركة. والحركة من مزايا آثار+،
   وتُفحص على الخادم من بايتات الملف (`isAnimated`) لا من امتداده.
   **ومقاسها من ١٢٠×١٢٠ إلى ٣٢٠×٣٢٠** (`ANIMATED_SIDE`) والحجم ٣ ميغا
   كما هو: ترفع بملفها بلا تصغير، فمقاسُها هو ما يُفكّ في ذاكرة كلّ
   جهازٍ يعرضه — وأكبرُ ما تُعرض فيه ١٠٤ بكسلاً، و١٠٢٤×١٠٢٤ بثلاثة
   ميغا كان مئة إطارٍ تُفكّ لقرصٍ بحجم ظفر. والحدّ في الشاشة وفي
   الخادم معاً — ولم يكن على خادم `apps/api` فحصُ مقاسٍ أصلاً.
83. **المحادثات تُكنس بعد ٣٠ يوماً** (`sweepOld`): حذفٌ من القاعدة لا
   إخفاءٌ في الاستعلام، ومع نبض الحضور لا بمهمّةٍ مجدولة على خادمٍ لا
   نملكه. ويُقال للمستخدم في شاشة المحادثات.
84. **حذف اللحظة يحذف صورتها**: علاقة الصورة `SetNull`، فحذف اللحظة وحده
   كان يترك بكسلاتها في القاعدة إلى الأبد.
85ب. **شريط التفاعل يلتفّ ولا يُقصّ**: خمسة وجوه، ففاصل، فإيموجيان،
   فـ«＋» — أعرضُ من شاشةٍ ضيّقة، فكان آخرُه يخرج عن الحافة وآخرُه هو
   بابُ لوحة الإيموجي. و`flexWrap` لا تمريرٌ أفقيّ: ما يُمرَّر إليه
   يحتاج أن يُكتشف، وبابُ ميزةٍ مدفوعة لا يُخبّأ.
85ج. **الإيموجي من إصداراتٍ يرسمها كل جهاز** (يونيكود ١١ فما دون):
   🫶 و🥹 و🫣 و🫂 تخرج مربّعاً فارغاً على أندرويد، وكانت الأولى منها
   أوّلَ الصفّ بجانب الفاصل — فمن ضغطها تفاعل بفراغ. واللوحة تحتاج
   `nestedScrollEnabled` لأنها تنزل داخل قائمةٍ تنزل.
85. **الإيموجي الحرّ لوحةٌ لا اثنان**: الاشتراك يَعِد بـ«كل كيبوردك»،
   واثنان ليسا كلَّ شيء. واللوحة في مكانَي التفاعل معاً — شريط الخط
   الزمني وصفحة اللحظة.
   و**ثمانيةٌ وأربعون ليست كيبورداً** أيضاً: صارت مجموعاتٍ لها عناوين
   (`lib/emoji.ts` ← `EMOJI_GROUPS`) تُمرَّر كما يُمرَّر كيبورد النظام،
   والعنوان `sticky` فوق صفوفه فلا تُقرأ اللوحة كومةً. والقائمة نسخةٌ
   واحدة في الويب والجوّال — وكانت الأربعة الحديثة (٨٥ج) قد نُزعت من
   الجوّال وبقيت في الويب.
72. **الثيم ثوبٌ لا خلفية** (`palette` على `StoreItem` + `themeVars`):
   من يشتريه يلبس التطبيقُ ألوانَه كلها — الأرضية والبطاقات والحبر
   واللمسة والشريطان. سبعة ألوان يضبطها المشرف، وما عداها يُشتقّ
   بـ`color-mix` فلا يُسأل عن درجاتِ لونٍ واحد. وخلفيةٌ تتبدّل وحدها
   ليست شيئاً يُشترى.
73. **اللوحة ثلاث درجات**: المالك (`role = ADMIN`)، وممنوحُ اللوحة
   (`adminScope = ALL`)، وممنوحُ المتجر وحده (`STORE`). والمنحُ للمالك
   وحده وإلا رفع المشرفُ نفسَه. والدرجة تُفحص في كل إجراء لا في العرض:
   `requireAdmin("store")` للمتجر و`requireAdmin()` لما سواه.
73ج. **النقاط عملة المتجر الوحيدة** (`COIN_HALALAS = 3`): كل ما في
   المتجر يُشترى بها ولا يُشترى شيءٌ بالريال مباشرةً، والاشتراك وحده
   يبقى بمالٍ حقيقي. والصرف ١٠٠٠ نقطة = ٣٠ ر.س، وهو مكتوبٌ في
   `packages/shared` وحده.
   ومن نفد رصيدُه يشحن: باقاتٌ في القاعدة (`CoinPack`) تُضاف وتُعدَّل
   وتُحذف من اللوحة كتصنيفات المتجر، وبابها `?s=store&v=packs`.
   و**الشراء لا يمرّ بنا**: السلع الرقمية تُباع عبر متجر المنصّة وحده،
   فـ`sku` معرّف المنتج هناك وهو الرابط بيننا وبينه — وباقةٌ بلا `sku`
   تبقى مسوّدةً لا تُعرض، فلا يُضغط زرٌّ لا يفتح شيئاً. ولا باب
   «اشترِ نقاط» في الخادم: لو وُجد لكان باب منحٍ مجّانيّ لمن يعرف كيف
   يرسل طلباً. والإيداع من حدث `NON_RENEWING_PURCHASE`، ويُفصل قبل
   منطق الاشتراك — حمولته بلا استحقاقٍ وبلا تاريخ انتهاء، فلو مرّت على
   ما تحته لقُرئت «اشتراكٌ منتهٍ» فأُوقف آثار+ لمن اشترى نقاط.
   وكل شحنةٍ صفٌّ في `CoinTopUp` بمفتاح الحدث فريداً: رصيدٌ بلا سجلٍّ
   لا يُجاب عنه سؤال «أين ذهبت نقاطي»، والتسليم «مرّةً على الأقل»
   فالقيد يمنع الإيداع مرّتين.
73ب. **رصيد آثار+ شهريٌّ لا فاتوريّ** (`dripPlusCredit` + `User.plusCreditAt`):
   الوعد «١٠٠٠ نقطة شهرياً»، ومن اشترك سنوياً يفوتر مرّةً واحدة — فربطُ
   الإيداع بحدث الفوترة يعطيه دفعةً بدل اثنتي عشرة. الأولى مع التفعيل،
   وما بعدها مع الكنس كل ثلاثين يوماً، والختم يتقدّم شهراً فلا تضيع
   دورةٌ بنوم الخادم. ومن أُوقف يُنسى ختمُه فيبدأ عند عودته دورةً
   جديدة.
74. **الدعم داخل التطبيق** (`SupportTicket`): الرسالة تُحفظ ويردّ عليها
   المشرف من اللوحة، ويقرأ صاحبها الردّ في مكان سؤاله. ومن الموقع تُكتب
   رسالةٌ بلا حساب (`/contact` ← `userId` فارغ مع اسمٍ وبريد): شرط
   المتجرين وسيلةَ تواصلٍ منشورة يبلغها من لا حساب له — أو من حُذف
   حسابه فلم يبقَ له باب — والردّ يذهب إلى بريده لأنّه بلا شاشةٍ يقرأ
   فيها. بريدٌ في صفحة
   «تواصل معنا» يعني رسالةً تخرج فلا يعرف أحد أوصلت أم لا — وهو أيضاً
   شرط متجر آبل: وسيلة تواصلٍ داخل التطبيق.
75. **«لك معانا» تحت الاسم في اللحظات** لا رقماً في إحصاءات الملف:
   الساعة يعرفها الجهاز، ومدّة العضوية لا. وصيغتها عربية بالمثنى
   والجمع (`membership`) لا رقماً عارياً بجانب «شهر».
76. **آثار+ نجمة** (`SparkIcon`): كانت تاجاً، والتاج يُقرأ رتبةً على
   الناس، والنجمة تُقرأ في كل لغة.
71. **الغلاف طبقةٌ واحدة في كل الشاشات** (`CoverLayer`): صورته ودرعه
   تحت قناع الذوبان نفسه، فيذوبان معاً. درعٌ خارج القناع ينتهي بكامل
   قتامته عند الحافة فيرسم الخطَّ الحادَّ الذي جاء القناع ليمحوه —
   وهذا ما كان يجعل غلاف الملف يبدو غير غلاف اللحظات. والذوبان قصير
   (آخر ١٦٪ فقط): ذوبانٌ طويل يخلط الغلاف بالثيم تحته فيُقرأ ضبابة.
70. **لا زرّ يفتح ما يفتحه تبويبٌ ظاهر**: زائدُ الأصدقاء كان يفتح
   «مقترحون»، وأيقونةُ المحادثات كانت تفتح الأصدقاء — وشاشة المحادثات
   محادثاتٌ فقط، لا نسخةٌ ثانية من تبويب الأصدقاء تحتها.
64. **خط المخطط عند `right: 27px`** = منتصف عمود الصور (`w-14`) داخل
   حاوية `px-5`. أي تغيير في عرض العمود أو الحشوة يُتبَع بتغيير الإزاحة،
   وإلا مشت الصور بجانب الخط لا عليه.
9. **الإقلاع**: الغلاف الداكن مع الشعار والعبارة، ثم يخرجان بتلاشٍ
   وارتفاع خفيف وتظهر خيارات الدخول. مرة واحدة لكل جلسة متصفح.
   ودخول المزوّدين (Apple / Google / Facebook) معروض لكنه **غير
   مفعّل** حتى تُسجَّل مفاتيح كل مزوّد؛ البريد هو العامل. وX أُلغي بطلب
   المالك. ولا بيانات حسابٍ تجريبيّ في الشاشة — لا مملوءةً في الحقول ولا
   مكتوبةً تحتها، ولو خلف شرط التطوير.

105. **لا نطاقَ يُكتب في الكود** (`SITE_URL` في `packages/shared`): النطاق
   لم يُحسم، والمكتوب اليوم يبقى في ملفٍّ منسيٍّ بعد أن يُحسم. يُقرأ من
   البيئة باسمه في كل مضيف — `SITE_URL` على الخادم، و
   `NEXT_PUBLIC_SITE_URL` في الويب، و`EXPO_PUBLIC_SITE_URL` على
   الجوّال. وفراغُه ليس عطلاً: زرّ اللوحة يختفي، والمشاركة تُرسل رقم
   العضوية بلا رابط، وبطاقة `/u` تبقى بروابطَ نسبيةٍ عبر `metadataBase`.
106. **خطوط العلامة تُحمَّل ولا تُترك للجهاز** (`theme/fonts.ts`): بلا
   تحميلٍ يرسم أندرويد بـRoboto وآبل بـSF Pro، فيخرج الاسم نفسه بثلاثة
   أشكال على ثلاث شاشات. IBM Plex Sans Arabic للمتن، وTajawal للعناوين،
   وMontserrat للاتيني — كالويب حرفاً بحرف.
   و**كلُّ وزنٍ عائلةٌ مستقلّة** حين تُحمَّل بـ`useFonts`: `fontWeight`
   فوقها لا يجلب الملفّ الثقيل. فالوزن يُترجَم إلى اسم عائلة، ويُحذف
   `fontWeight` — و`components/type.tsx` غلافٌ واحد لكل نصّ، فكلُّ ملفّ
   يستورد `Text` منه لا من `react-native`. وما يرسمه الشريطُ السفلي
   بنفسه يكتب عائلته بيده (`tabBarLabelStyle`).
107. **`elevation` لأندرويد و`shadow*` لآبل** — لا يغني أحدهما عن الآخر.
   وظلّ الشريط السفلي إلى **أعلى** (`height: -2`): الشريط في أسفل
   الشاشة، فظلٌّ نازلٌ منه يقع خارجها.
108. **ما يُحسب من عرض الشاشة لا يُكتب رقماً**: نصفُ قطر قوس النشر كان
   ٢٤٦ مكتوباً، فيخرج أدنى أصنافه عن الحافة اليسرى على شاشةٍ ٣٢٠ فيُضغط
   على فراغ. صار `radiusFor(width)`. وأصغرُ ما يُدعم ٣٢٠×٥٦٨.
109. **الإشراف على المحتوى صلاحيةٌ تُمنح واحداً واحداً** (`User.canModerate`):
   من يملكها يقرأ لحظات أيّ حساب بلا صداقة ويحذف ما يخالف منها —
   للتصرّف في بلاغ. **مستقلّةٌ عن `adminScope`** لا لازمةٌ عنها: من يدير
   المتجر لا يحتاج أن يقرأ لحظات الناس. والمالك يملكها بدوره لا بالحقل.
   وبابُها **ثانٍ ظاهرٌ** لا توسعةٌ لـ`visibleWhere()` (القاعدة ٢٣):
   `moderatedMomentsOf` خلف `requireModerator` وحدها، والاستعلام العاديّ
   يبقى على شرط الرؤية — فلا يحمل ثقباً يُفتح بحقلٍ في صفّ القارئ.
   وتُقرأ من الصفّ لا من التوكن، فبالسحب يُغلق البابُ في اللحظة نفسها.
   وكلُّ حذفٍ في `ModerationLog` باسم صاحبه: سلطةٌ بلا أثرٍ مكتوب لا
   يُسأل عنها أحد.

110. **محتوى الموقع العام من اللوحة** (`SiteText` + `SiteImage` + `SocialLink`):
   كل نصٍّ في صفحة الهبوط وذيلِ الموقع يُحرَّر من `/admin?s=site` بلا
   نشر نسخة، وصورةُ الرأس تُرفع وتُنزع.
   و**الافتراضيّ في الكود لا بذرةٌ في القاعدة** (`lib/site.ts`): لا
   يُكتب صفٌّ إلا حين يعدّله المشرف، فقاعدةٌ جديدة تعرض الموقع كاملاً
   بلا صفٍّ واحد — ونشرٌ على قاعدةٍ عامرة لا يمسّ ما حرّره أحد.
   **والمساواةُ للافتراضيّ حذفٌ لا كتابة**: من أعاد النصّ إلى أصله
   يُمحى صفُّه فيعود يتبع الكود إن تغيّر فيه لاحقاً، ولا يتجمّد على
   نسخةٍ نُسخت إلى القاعدة بلا قصد.
   وبلا صورةِ رأسٍ تُرسم اللوحة المرسومة (`HeroArt`): موقعٌ بلا رأسٍ
   أسوأ من رأسٍ مرسوم.
   والرسومُ في الكود لا في القاعدة: صفُّ الرابط يحمل اسم المنصّة
   والعنوان وهما ما يتغيّر، وSVG في حقلِ نصٍّ حقلٌ يُلصق فيه أيّ شيء
   ويُرسم في صفحةٍ عامّة. والرابط يُفحص بروتوكولُه — `z.url()` يقبل
   `javascript:` لأنّه يفحص الشكل لا المعنى.
111. **«تابعنا» مكانٌ واحد لوجهتين**: الصفوف نفسها تُعرض في ذيل الموقع
   وفي أسفل شاشة الخصوصية بالتطبيق (`/v1/site/social`) — فحسابٌ جديد
   يظهر فيهما بلا نشر نسخةٍ من أيّهما. ومكانُه في التطبيق أسفلُ
   الخصوصية لا الخط الزمني: لا استكشاف عام في آثار (القاعدة ٢)،
   وحساباتنا خارجه لا تجرّه إليه.
112. **مكوّنُ عميلٍ لا يستورد ملفاً فيه `prisma`**: لوحةُ تحرير الموقع
   استوردت `lib/site.ts` لقائمة المنصّات، فجرّت القاعدة إلى حزمة
   المتصفّح وسقط البناء بـ«لا أجد `dns`» — خطأٌ لا يقول شيئاً عن سببه.
   فما يقرؤه الطرفان يجلس وحده (`lib/platforms.ts`).
113. **دخولُ صفحة الهبوط بتأخّرٍ متدرّج** (`.rise` في `globals.css`):
   الشعار ثم الاسم ثم العبارة ثم المتن ثم الزرّان — كقوس النشر في
   التطبيق. و`backwards` لازمة: بدونها يومض العنصر بكامل وضوحه قبل أن
   يبدأ تأخّرُه فتُرى الشاشة كاملةً ثم تختفي لتدخل. ومن طلب «قلّل
   الحركة» لا يُعرض عليه شيءٌ يتحرّك (القاعدة ٣٦).

114. **الإشراف يجري حيث يُقرأ المحتوى لا في لوحةٍ منفصلة**: زرُّ الحذف
   داخل لوحة التفاعل نفسها (`MomentBar` بـ`moderate`) — البلاغ يصل على
   منشور، فيفتحه المشرف حيث يقرؤه الناس ويحكم في مكانه، ومعه زرُّ
   البلاغ في اللوحة نفسها. وتصفّحُ آلاف الحسابات في لوحة تحكّم للوصول
   إلى منشورٍ ليس طريقاً.
   **والبابان مختلفان وإن تشابه الزرّان**: صاحبُها `/v1/moments/:id`،
   والمشرفُ `/v1/moderation/moments/:id` خلف `requireModerator` ومعه
   سجلّ. فلا يُوسَّع بابُ الصاحب ليقبل غيره.
115. **قائمةُ الحسابات تُبحَث ولا تُتصفَّح**: عند ألفٍ لا تُقرأ بالعين.
   البحث برقم العضوية بالمطابقة التامّة — هو ما يُقال ويُكتب (القاعدة
   ١٥)، و«١٢» بالتضمين تجلب ١٢٠ و٣١٢ — أو بجزءٍ من الاسم أو البريد،
   وبلا بحثٍ أحدثُ خمسين. **والأرقام العربية-الهندية تُحوَّل**: من يقرأ
   رقمه «٤٢» في اللوحة ينسخه كما رآه.
   وقسمُ «الصلاحيات» يقرأ استعلامَه الخاصّ (`staff`) لا القائمة
   المقصوصة: مشرفٌ رقمُه ٣ كان يختفي من صفحته بمجرّد أن يُسجَّل بعده
   خمسون.
116. **الإيقاف المؤقّت يمنع الكتابة ولا يمحو التطبيق**
   (`User.suspendedUntil`): الحساب قائمٌ بلحظاته وأصدقائه ورصيده،
   ويُمنع من النشر والتعليق والرسائل والقصص (`requireActive` على مسارات
   الكتابة وحدها) ومن إصدار جلسةٍ جديدة — وتبقى له القراءة، فمن مُنع من
   النشر لا يُمنع من رؤية ما قاله له الناس ولا من قراءة سبب وقفه.
   وفحصُه في مسارات الكتابة لا في `requireAuth`: الأخير يجري مع كل
   تمريرة خطٍّ زمنيّ، فاستعلامٌ زائد فيه ثمنٌ بلا مقابل.
   **وجلساتُه القائمة تُبطَل عند الإيقاف**: توكن التجديد عمرُه ثلاثون
   يوماً، ولولا إبطالُه لبقي داخلاً بجلسةٍ صدرت قبل القرار.
   والمدّة قائمةٌ مغلقة لا حقلُ أيام: حقلٌ حرّ يقبل «٩٩٩٩» بزلّة إصبع،
   وإيقافُ سبعٍ وعشرين سنةً حذفُ حسابٍ بلا اسمه. ولا يُوقِف المشرفُ
   مشرفاً ولا نفسه.
   والرسالة تقول ثلاثاً: أنّه موقوف، ومتى ينتهي، ولماذا — إيقافٌ بلا
   مدّةٍ يُقرأ حذفاً للحساب، وبلا سببٍ يُقرأ خطأً فيُراسَل الدعم عبثاً.
117. **ملفُّ `"use server"` لا يُصدِّر إلا دوالَّ غير متزامنة**: تصديرُ
   ثابتٍ منه يسقط عند التشغيل بـ«A "use server" file can only export
   async functions» — ولا يقوله المترجم، فالبناء ينجح والشاشة تسقط.
   فما يقرؤه الإجراءُ ومكوّنُ العميل معاً يجلس وحده (`lib/suspend.ts`).
118. **التاريخ ميلاديٌّ بالعربية في كل مكان**: `ar-SA` وحدها تُخرجه
   هجرياً («٨ ربيع الآخر ١٤٤٨»)، وشاشةٌ تخلط التقويمين تُقرأ تاريخين لا
   واحداً — و`-u-ca-gregory` تحسمه. و`relative()` لما مضى وحده: تردّ
   «الآن» لكل تاريخٍ آتٍ، فيُقرأ إيقافُ ثلاثة أيام «ينتهي الآن»
   (`untilDay` لما هو آتٍ).

## اتجاه التطبيق على الجوّال

الواجهة عربية من اليمين إلى اليسار في الويب والتطبيق سواء، والاتجاه
يُفرض من جذر الشجرة لا يُكتب في كل صفّ: `I18nManager` للجهاز،
و`direction: "rtl"` على الجذر ليقلبها Yoga فوراً بلا إعادة تشغيل،
و`dir="rtl"` على المستند في معاينة الويب لأن `I18nManager` هناك قشرةٌ
فارغة. فكلّ صفٍّ يُكتب `row` لا `row-reverse` — والمعكوس ينقلب مرّتين
فيعود لاتينياً. وما كان يُقال من أن شريط التبويبات يحتاج `row-reverse`
غيرُ صحيح: أصنافه في حاويةٍ داخلية لا نملك نمطها، وهو يتبع اتجاه الجذر.

و**`left`/`right` تبقى يساراً ويميناً**: React Native يقلبهما — ومعهما
`textAlign` — من نفسه حين يكون الاتجاه من اليمين، وهذا قلبٌ ثانٍ فوق قلب
Yoga، فالشجرة مكتوبةٌ على مقاس الويب حيث `right` فوق جذرٍ `rtl` يمينٌ
حقيقيّ. فبالقلب التلقائي طار زرّ النشر إلى اليسار وأصنافه خارج الشاشة،
وطار بابا العدسات، وانزاح نصّ «تعديل الملف» و«إكسسواراتي» و«الخصوصية».
وإطفاؤه `swapLeftAndRightInRTL(false)` — لا كتابة `start`/`end` في ستّين
موضعاً لا تعني شيئاً في معاينة الويب.

**والاتجاه يُعاد فرضُه على حاوية كل شاشة** (`contentStyle` في المكدّس،
وعلى `contentContainerStyle` في شاشات التمرير الثلاث): الجذر يحمله لكنّ
الوراثة لم تصل إلى كل شجرةٍ على الجهاز — صفوف `flexWrap` في
«إكسسواراتي» كانت ترتصّ من اليسار والمعاينة تعرضها صحيحة، فلا يُكشف
إلا على جهاز. وتكرارُه حيث وصلت الوراثة لا يضرّ.

**والسحب للتحديث يُلتقط في طور الهبوط** (`onMoveShouldSetPanResponderCapture`):
الطور الصاعد يُسأل بعد القائمة، والقائمة تأخذ الإيماءة لنفسها فلا يصل
شيء — فكان السحب لا يحرّك الغلاف على الجهاز وإن عمل باللمس المُصطنع في
المتصفّح.

**والتنقّل مكدّسٌ لا `Slot`** في `apps/mobile/app/_layout.tsx`: `Slot`
يعرض الشاشة الحاليّة بلا تاريخ، فـ`canGoBack()` يردّ «لا» دائماً ويسقط
كلُّ رجوعٍ على الوجهة المكتوبة في الرأس — ومعظمها «/» — فيرجع من أيّ شاشة
إلى اللحظات لا إلى ما قبلها. والمكدّس يحفظ الطريق، ومعه إيماءةُ الحافة
وزرُّ أندرويد.

ومكانه `plugins/with-rtl.js` **على الجهازين معاً** — `MainApplication`
لأندرويد و`AppDelegate` لآبل — لا جافاسكربت وحدها: القيم تُقرأ عند **بدء
السطح**، وذلك قبل أوّل سطر جافاسكربت، فأوّل تشغيلٍ بعد التثبيت كان يخرج
بالافتراضيّات ثم يُصلح نفسه في التشغيل الثاني بلا سبب ظاهر. والنداء في
جافاسكربت يبقى للمعاينة، **بعلامة استفهام**: `react-native-web` قشرةٌ لا
`swapLeftAndRightInRTL` فيها، ونداؤُها هناك يرمي قبل أن يُضبط اتجاه
المستند في السطر التالي فتسقط المعاينة كلّها.

## الستاك والأعراف

- Next.js 16 (App Router) + React 19 + TypeScript صارم.
- Prisma 7 فوق Postgres عادي، عبر `@prisma/adapter-pg`.
- Tailwind 4 بإعداد CSS-first — الرموز في `@theme` داخل `globals.css`.
- المصادقة: جلسات JWT (jose) في كوكي httpOnly، وتجزئة كلمات المرور
  بـ scrypt من مكتبة Node القياسية — بلا اعتماديات إضافية.
- كل منطق العمل في `src/app/actions.ts` كإجراءات خادم. **كل إجراء
  يتحقق من المصادقة والصلاحية بنفسه** — إجراءات الخادم قابلة
  للاستدعاء مباشرة بـPOST، لا تعتمد على إخفاء الواجهة.
- الواجهة عربية RTL. التعليقات والنصوص بالعربية، وأسماء الكود
  بالإنجليزية.
- الأرقام تُعرض عربية-هندية عبر `src/lib/format.ts`.
- المبالغ تُخزَّن بالهللات كأعداد صحيحة.

قبل كتابة أي كود: **اقرأ الدليل المناسب في `node_modules/next/dist/docs/`**.
نسخة Next هنا أحدث من بيانات تدريبك — `cookies()` و `params` غير متزامنة،
وقد تكون هناك تغييرات أخرى.

## ما هو مؤقت في النموذج

هذه بدائل مقصودة لمرحلة التحقق، وموثّقة في README. لا تعاملها كنواقص
تصلحها تلقائياً — استبدالها قرار مرحلة، لا تنظيف كود:

- الصور تدرّجات CSS (`imageSpec`) لا ملفات مرفوعة.
- الاشتراك يُفعَّل بضغطة بلا دفع **في التطوير وحده** (`ALLOW_FAKE_PLUS`)،
  وكان بابه مفتوحاً للجميع فأُغلق. والدفع الحقيقي يمرّ بـIAP لآبل و
  Google Play Billing عبر RevenueCat (`BILLING.md`): الجهاز يشتري،
  والخادم لا يفعّل إلا بحدثٍ من RevenueCat على
  `/v1/webhooks/revenuecat`. وApple Pay ومدى ممنوعتان للسلع الرقمية
  وتؤدّيان لإزالة التطبيق.
- الإيموجي الحر قائمة جاهزة بدل كيبورد النظام.
- الحضور المنتهي يُخفى بشرط في الاستعلام لا بمهمة مجدولة. في الإنتاج:
  `pg_cron`.
- لا إشعارات — تحتاج تطبيقاً أصلياً.

## التشغيل

```bash
npm install
cp .env.example .env          # DATABASE_URL و AUTH_SECRET
npx prisma migrate deploy
npm run db:seed
npm run dev
```

حسابات التجربة كلها بكلمة المرور `athar1234`:
`mohammed@` (عنده إشارة معلّقة) · `noura@` (مشتركة، تلبس إطاراً) ·
`naif@` (عنده حضور فعّال) · `sultan@` — كلها على `@athar.test`.

قبل أي تسليم: `npm run typecheck` و `npx next build`، ثم **شغّل التطبيق
وجرّب المسار فعلياً في متصفح**. البناء الناجح ليس دليلاً على أن الشاشة
تعمل.

## اعتبارات نظامية

- بيانات الموقع والصور والعلاقات = بيانات شخصية تحت نظام حماية البيانات
  السعودي. الخروج بها من المملكة مقيّد، وسدايا لم تنشر قائمة الدول ذات
  الحماية المكافئة. لهذا القاعدة Postgres عادي: نقلها إلى خادم داخل
  المملكة `pg_dump` و `pg_restore`، لا مشروع ترحيل.
- متجر آبل يشترط لأي تطبيق فيه محتوى مستخدمين: فلترة محتوى، إبلاغ عن كل
  منشور، حظر مستخدم، ووسيلة تواصل منشورة، وحذف الحساب داخل التطبيق.
  **الخمسة مبنيّةٌ الآن**: الفلترة قائمةُ كلماتٍ تُفحص بها اللحظات
  والتعليقات والرسائل، والإبلاغ على كل لحظةٍ وقصةٍ ورسالة يُحسم في
  اللوحة بيد، والحظر في الاتجاهين، ووسيلة التواصل داخل التطبيق ومن
  الموقع، وحذف الحساب في تبويب «أنا» و`/delete-account`.
  وما بقي من شروط النشر: الدفع عبر IAP، وتأكيد البريد.
